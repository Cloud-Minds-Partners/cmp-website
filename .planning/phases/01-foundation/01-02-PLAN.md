---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - src/styles/global.css
  - src/layouts/Base.astro
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "Space Grotesk Variable and DM Sans Variable render from local npm packages — zero Google Fonts requests"
    - "Tailwind 4 utilities `bg-navy-0`, `text-cream-0`, `text-blue`, `font-display`, `font-body` work in any .astro file without a CSS import"
    - "Body defaults (navy-0 background, cream-0 text, DM Sans body font, 1.55 line-height, smooth scroll) match mock-26/site.css"
    - "The v4 dark-tech tokens (`--color-bg-base`, `--color-ink`, `--font-mono: JetBrains Mono`) are fully gone from global.css"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Complete @theme block with mock-26 tokens + fontsource imports + @layer base body defaults"
      contains: "--color-navy-0: #050E1D"
      min_lines: 80
    - path: "src/layouts/Base.astro"
      provides: "Base layout with Google Fonts <link> tags removed, locale-aware lang attribute"
      contains: "lang={lang}"
  key_links:
    - from: "src/styles/global.css"
      to: "Tailwind 4 utility classes"
      via: "@theme CSS variable → auto-generated utility"
      pattern: "--color-navy-0"
    - from: "src/layouts/Base.astro"
      to: "src/styles/global.css"
      via: "<link rel='stylesheet'> or Astro global import"
      pattern: "global.css"
---

<objective>
Replace the existing v4 dark-tech design tokens, remove Google Fonts CDN dependencies, install local variable fonts, and set mock-26 body defaults.

Purpose: Every Phase 2 component will use Tailwind utilities like `bg-navy-0`, `text-cream-0`, `font-display`. Those utilities only exist when the `@theme` block contains the correctly named tokens. This plan makes the substrate correct before any component is built.

Output: `src/styles/global.css` with mock-26 tokens + fontsource imports + base styles. `src/layouts/Base.astro` with Google Fonts removed and locale-aware `lang` prop.
</objective>

<execution_context>
@/Users/edgardabreu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edgardabreu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Current global.css uses these WRONG token names — they must all be replaced: -->
<!-- --color-bg-base, --color-accent, --color-ink, --font-body: "Inter", --font-mono: "JetBrains Mono" -->

<!-- Current Base.astro (lines 39-44) contains these Google Fonts <link> tags to DELETE: -->
<!--
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Inter:..." rel="stylesheet" />
-->

<!-- Target @theme token names (from mock-26/tokens.css, verified in RESEARCH.md): -->
<!-- --color-navy-0: #050E1D  → Tailwind utility: bg-navy-0, text-navy-0 -->
<!-- --color-blue: #2D6BE4    → Tailwind utility: bg-blue, text-blue -->
<!-- --color-cream-0: #F5F1E8 → Tailwind utility: bg-cream-0, text-cream-0 -->
<!-- --font-display: "Space Grotesk Variable" → Tailwind utility: font-display -->
<!-- --font-body: "DM Sans Variable"          → Tailwind utility: font-body -->

<!-- CRITICAL: Token name determines utility class. --color-navy-0 → bg-navy-0 (not bg-cmp-navy-0). -->
<!-- Do NOT use cmp- prefix in token names. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install variable fonts and replace global.css</name>
  <files>src/styles/global.css, package.json</files>
  <action>
**Step 1 — Install variable font packages:**
```bash
cd /Users/edgardabreu/Projects/CODE/cmp-website
npm install @fontsource-variable/space-grotesk @fontsource-variable/dm-sans
```

These are the variable font packages (NOT `@fontsource/space-grotesk` static weight files). Variable font covers weights 300–700 in a single file. Default `font-display: swap` is built into the package.

**Step 2 — Replace `src/styles/global.css` entirely.**

The current file uses v4 dark-tech token names. This is a FULL REPLACE, not a merge. The target content is exactly as specified in RESEARCH.md §Complete global.css Structure:

```css
/* Source: Tailwind 4 docs + mock-26/tokens.css + fontsource.org */
@import "tailwindcss";
@plugin "@tailwindcss/typography";
@import "@fontsource-variable/space-grotesk/wght.css";
@import "@fontsource-variable/dm-sans/wght.css";

@theme {
  /* Fonts — Space Grotesk (display) + DM Sans (body) */
  /* IMPORTANT: Variable font family name is "Space Grotesk Variable" (not "Space Grotesk") */
  --font-display: "Space Grotesk Variable", "Inter", system-ui, sans-serif;
  --font-body:    "DM Sans Variable", system-ui, -apple-system, sans-serif;
  /* NO --font-mono — mock-26 v0.8 removes typewriter labels */

  /* Navy stack (from mock-26/tokens.css) */
  --color-navy-0: #050E1D;
  --color-navy-1: #0A1E38;
  --color-navy-2: #122849;
  --color-navy-3: #1B3A5C;
  --color-navy-4: #234A75;
  --color-black:  #000000;

  /* Brand blue */
  --color-blue:        #2D6BE4;
  --color-blue-bright: #4A8FE7;
  --color-blue-deep:   #1D4289;

  /* Cream text + backgrounds */
  --color-cream-0:    #F5F1E8;
  --color-cream-1:    #C8D8F0;
  --color-cream-2:    #8A94A8;
  --color-cream-3:    #5A6478;
  --color-cream-bg:   #F5F1E8;
  --color-cream-bg-2: #EDE6D5;
  --color-cream-bg-3: #FAF6EC;

  /* Hairlines */
  --color-line-0: #14213D;
  --color-line-1: #1F2E54;

  /* Status */
  --color-status-live: #4A8FE7;
  --color-status-prod: #22C55E;
  --color-status-warn: #F5A623;

  /* Layout */
  --max-w: 1320px;
  --gutter: clamp(20px, 4vw, 56px);

  /* Transitions */
  --t-fast: .18s cubic-bezier(.4,0,.2,1);
  --t-base: .32s cubic-bezier(.4,0,.2,1);
  --t-slow: .6s cubic-bezier(.4,0,.2,1);
}

@layer base {
  /* Source: mock-26/site.css */
  *, *::before, *::after { box-sizing: border-box; }
  html, body { margin: 0; padding: 0; }
  html {
    scroll-behavior: smooth;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
  }
  body {
    background: var(--color-navy-0);
    color: var(--color-cream-0);
    font-family: var(--font-body);
    font-feature-settings: "ss01";
    line-height: 1.55;
    overflow-x: hidden;
  }
  a { color: inherit; text-decoration: none; }
  img { display: block; max-width: 100%; }
  button { font: inherit; cursor: pointer; border: 0; background: transparent; color: inherit; }
  ::selection { background: var(--color-blue); color: var(--color-cream-0); }
}

/* Layout wrapper (from mock-26/site.css .wrap) */
.wrap {
  max-width: var(--max-w);
  margin: 0 auto;
  padding: 0 var(--gutter);
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

**What NOT to do:**
- Do NOT keep any `--color-bg-base`, `--color-ink`, `--color-accent`, `--font-mono: "JetBrains Mono"` tokens — they are v4 dark-tech artifacts
- Do NOT use `@fontsource/space-grotesk/400.css` (static per-weight) — use the `-variable` package
- Do NOT add `cmp-` prefix to token names — `--color-navy-0` generates `bg-navy-0`, which is what mock-26 HTML uses
  </action>
  <verify>
    <automated>cd /Users/edgardabreu/Projects/CODE/cmp-website && grep -q -- '--color-navy-0: #050E1D' src/styles/global.css && grep -q -- '--color-blue: #2D6BE4' src/styles/global.css && ! grep -q -- '--color-bg-base' src/styles/global.css && test -d node_modules/@fontsource-variable/space-grotesk && test -d node_modules/@fontsource-variable/dm-sans && echo "FOUND-02 + FOUND-03 source checks PASS"</automated>
  </verify>
  <done>
`@fontsource-variable/space-grotesk` and `@fontsource-variable/dm-sans` installed. `src/styles/global.css` contains the full mock-26 @theme block including `--color-navy-0: #050E1D` and `--color-blue: #2D6BE4`, fontsource imports, @layer base body defaults. Zero v4 dark-tech tokens remain. `scroll-behavior: smooth` and `background: var(--color-navy-0)` are in @layer base.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Google Fonts from Base.astro and add locale-aware lang prop</name>
  <files>src/layouts/Base.astro</files>
  <action>
Read `src/layouts/Base.astro` first. Then make two targeted edits:

**Edit 1 — Remove Google Fonts `<link>` tags (lines 39-44 per RESEARCH.md):**

Delete ALL three of these lines:
```astro
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link
  href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Space+Grotesk:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap"
  rel="stylesheet"
/>
```

If the exact lines differ slightly from the pattern above, remove any `<link>` tag whose href contains `fonts.googleapis.com`, `fonts.gstatic.com`, or `fonts.google.com`.

After removal: `grep -r "googleapis" src/layouts/Base.astro` must return nothing.

**Edit 2 — Add locale-aware `lang` prop:**

In the Astro frontmatter, destructure a `lang` prop with default `'en'`:
```astro
---
// existing imports...
const { lang = 'en', ...otherProps } = Astro.props;
---
```

Update `<html lang="en">` to `<html lang={lang}>`.

**What NOT to change:** Do not touch the title, description, or other meta tags in Base.astro. Those are out of scope for Phase 1 (Phase 2/3/4 handle SEO). Do not add i18n imports — this prop is a simple string passthrough that Phase 3 pages will supply.

**Validation:**
After edits, run `npm run build` from project root. Build must succeed (exit code 0) with zero TypeScript errors. A successful build confirms the Google Fonts removal does not break the layout and the locale prop is typed correctly.
  </action>
  <verify>
    <automated>cd /Users/edgardabreu/Projects/CODE/cmp-website && ! grep -q "googleapis" src/layouts/Base.astro && grep -q 'lang={lang}' src/layouts/Base.astro && echo "Base.astro PASS"</automated>
  </verify>
  <done>
`src/layouts/Base.astro` has zero references to `fonts.googleapis.com` or `fonts.gstatic.com`. `<html lang={lang}>` uses the prop. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build and run FOUND-02/03/04 shell checks</name>
  <files></files>
  <action>
Run the Astro build, then execute the relevant subset of the Phase 1 validation script to confirm the three requirements are met.

```bash
cd /Users/edgardabreu/Projects/CODE/cmp-website
npm run build
```

Build must exit 0 with zero TypeScript errors. If it fails, fix before proceeding.

Then run the specific checks from `scripts/validate-phase-1.sh` covering FOUND-02, FOUND-03, FOUND-04:

```bash
cd /Users/edgardabreu/Projects/CODE/cmp-website

# FOUND-02 (4 checks)
grep -q -- '--color-navy-0: #050E1D' src/styles/global.css && echo "PASS: navy-0 token" || echo "FAIL: navy-0 token"
grep -q -- '--color-blue: #2D6BE4' src/styles/global.css && echo "PASS: blue token" || echo "FAIL: blue token"
! grep -q -- '--color-bg-base' src/styles/global.css && echo "PASS: no v4 token" || echo "FAIL: v4 token still present"
grep -qr -- '--color-navy-0' dist/ --include="*.css" && echo "PASS: token in build CSS" || echo "FAIL: token not in build CSS"

# FOUND-03 (4 checks)
test -d node_modules/@fontsource-variable/space-grotesk && echo "PASS: space-grotesk pkg" || echo "FAIL"
test -d node_modules/@fontsource-variable/dm-sans && echo "PASS: dm-sans pkg" || echo "FAIL"
grep -q '@fontsource-variable/space-grotesk' src/styles/global.css && echo "PASS: fontsource import" || echo "FAIL"
! grep -qr "fonts.googleapis.com" dist/ --include="*.html" && echo "PASS: no googleapis in HTML" || echo "FAIL: googleapis still in HTML"

# FOUND-04 (2 checks)
grep -q 'scroll-behavior: smooth' src/styles/global.css && echo "PASS: smooth scroll" || echo "FAIL"
grep -q 'background: var(--color-navy-0)' src/styles/global.css && echo "PASS: body bg" || echo "FAIL"
```

All 10 checks must print "PASS". If any print "FAIL", fix the issue before marking this task done. These checks will not re-run — no silent skips allowed.
  </action>
  <verify>
    <automated>cd /Users/edgardabreu/Projects/CODE/cmp-website && npm run build 2>&1 | tail -5 && grep -q -- '--color-navy-0: #050E1D' src/styles/global.css && grep -q -- '--color-blue: #2D6BE4' src/styles/global.css && ! grep -q -- '--color-bg-base' src/styles/global.css && ! grep -qr "fonts.googleapis.com" dist/ --include="*.html" && echo "ALL FOUND-02/03/04 checks PASS"</automated>
  </verify>
  <done>
`npm run build` exits 0. All 10 FOUND-02/03/04 shell checks pass. Built CSS contains `--color-navy-0`. `global.css` contains `--color-blue: #2D6BE4`. No googleapis URLs in built HTML.
  </done>
</task>

</tasks>

<verification>
Full wave check after plan completes:
```bash
cd /Users/edgardabreu/Projects/CODE/cmp-website
# FOUND-02 token checks
grep -q -- '--color-navy-0: #050E1D' src/styles/global.css
grep -q -- '--color-blue: #2D6BE4' src/styles/global.css
! grep -q -- '--color-bg-base' src/styles/global.css
grep -qr -- '--color-navy-0' dist/ --include="*.css"
# FOUND-03 font checks
test -d node_modules/@fontsource-variable/space-grotesk
test -d node_modules/@fontsource-variable/dm-sans
grep -q '@fontsource-variable/space-grotesk' src/styles/global.css
! grep -qr "fonts.googleapis.com" dist/ --include="*.html"
# FOUND-04 base style checks
grep -q 'scroll-behavior: smooth' src/styles/global.css
grep -q 'background: var(--color-navy-0)' src/styles/global.css
# Base.astro
! grep -q "googleapis" src/layouts/Base.astro
grep -q 'lang={lang}' src/layouts/Base.astro
echo "Plan 02 complete"
```
</verification>

<success_criteria>
- `npm run build` succeeds with zero TypeScript errors
- `src/styles/global.css` contains the complete mock-26 @theme block, fontsource imports, @layer base — no v4 dark-tech tokens remain
- `src/styles/global.css` contains `--color-navy-0: #050E1D` and `--color-blue: #2D6BE4`
- `@fontsource-variable/space-grotesk` and `@fontsource-variable/dm-sans` are in package.json dependencies
- `src/layouts/Base.astro` has zero googleapis `<link>` tags and uses `lang={lang}` prop
- Built CSS in `dist/` contains `--color-navy-0`
- Built HTML in `dist/` contains zero `fonts.googleapis.com` references
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
