---
phase: 03-pages
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase3-pages.spec.ts
  - tests/phase3-collections.spec.ts
  - tests/phase3-i18n.spec.ts
  - src/pages/platforms/[slug].astro
  - src/pages/image-pipeline-test.astro
  - src/pages/newsletter.astro
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
  - PAGE-06
  - PAGE-07
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - I18N-03
  - I18N-04
  - I18N-05

must_haves:
  truths:
    - "Three Playwright spec files exist and import from @playwright/test"
    - "image-pipeline-test.astro is deleted from src/pages/"
    - "newsletter.astro is deleted from src/pages/"
    - "platforms/[slug].astro returns 301 redirect to /platforms#platforms-<slug>"
  artifacts:
    - path: "tests/phase3-pages.spec.ts"
      provides: "PAGE-01..07 smoke tests (7 tests tagged @smoke)"
      min_lines: 60
    - path: "tests/phase3-collections.spec.ts"
      provides: "CONT-02..06 collection route smoke tests"
      min_lines: 50
    - path: "tests/phase3-i18n.spec.ts"
      provides: "I18N-03..05 routing and translation tests"
      min_lines: 40
  key_links:
    - from: "playwright.config.ts"
      to: "tests/phase3-*.spec.ts"
      via: "testDir: './tests'"
      pattern: "testDir"
    - from: "tests/phase3-pages.spec.ts"
      to: "/ /advisory /development /intelligence /platforms /team /contact"
      via: "page.goto(url)"
      pattern: "page.goto"
---

<objective>
Plant Wave 0 infrastructure before any page work begins.

Purpose: Playwright spec stubs set the verification target for every subsequent wave. Deleting scaffolding files (image-pipeline-test.astro, newsletter.astro) and wiring the platforms redirect prevents dead routes.
Output: 3 Playwright spec files, deleted scaffolding, platforms redirect.
</objective>

<execution_context>
@/Users/edgardabreu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edgardabreu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pages/03-VALIDATION.md
@.planning/phases/03-pages/03-RESEARCH.md

<interfaces>
<!-- Existing Playwright config — no changes needed, just add spec files -->
playwright.config.ts:
  testDir: './tests'
  baseURL: 'http://localhost:4321'
  webServer: { command: 'npx astro dev', url: 'http://localhost:4321' }

<!-- Existing spec pattern (from tests/comp-01-header.spec.ts) -->
import { test, expect } from '@playwright/test';
test.use({ baseURL: 'http://localhost:4321' });
test.describe('NAME @smoke', () => {
  test('description', async ({ page }) => { ... });
});
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plant Playwright spec stubs (pages, collections, i18n)</name>
  <files>tests/phase3-pages.spec.ts, tests/phase3-collections.spec.ts, tests/phase3-i18n.spec.ts</files>
  <action>
Create three Playwright spec files. These are STUBS — tests will fail with "could not find element" etc. until Wave 1/2 pages are built, but the file must be valid TypeScript that imports from '@playwright/test'. Tests skip gracefully on non-200 if page does not exist yet (use `test.skip` or expect(status).toBe(200) which will naturally fail and be fixed in later waves).

**tests/phase3-pages.spec.ts** (covers PAGE-01..07, tagged @smoke):
```typescript
import { test, expect } from '@playwright/test';

test.describe('Phase 3 pages @smoke', () => {
  test('PAGE-01: home returns 200, hero has 3 slides', async ({ page }) => {
    const res = await page.goto('/');
    expect(res?.status()).toBe(200);
    const slides = page.locator('[data-variant="home"] .slide');
    await expect(slides).toHaveCount(3);
  });

  test('PAGE-02: advisory returns 200, 6 cap-cards', async ({ page }) => {
    const res = await page.goto('/advisory');
    expect(res?.status()).toBe(200);
    const cards = page.locator('.cap-card');
    await expect(cards).toHaveCount(6);
  });

  test('PAGE-03: development returns 200, 3 role-cards', async ({ page }) => {
    const res = await page.goto('/development');
    expect(res?.status()).toBe(200);
    const roles = page.locator('.role-card');
    await expect(roles).toHaveCount(3);
  });

  test('PAGE-04: intelligence returns 200, featured memo section present', async ({ page }) => {
    const res = await page.goto('/intelligence');
    expect(res?.status()).toBe(200);
    await expect(page.locator('.featured')).toBeVisible();
  });

  test('PAGE-05: platforms returns 200, 4 product sections', async ({ page }) => {
    const res = await page.goto('/platforms');
    expect(res?.status()).toBe(200);
    const products = page.locator('.product');
    await expect(products).toHaveCount(4);
  });

  test('PAGE-06: team returns 200, partner names in DOM', async ({ page }) => {
    const res = await page.goto('/team');
    expect(res?.status()).toBe(200);
    const body = await page.textContent('body');
    expect(body).toContain('Edgard');
    expect(body).toContain('Gustavo');
  });

  test('PAGE-07: contact returns 200, email link present', async ({ page }) => {
    const res = await page.goto('/contact');
    expect(res?.status()).toBe(200);
    const emailLink = page.locator('a[href*="info@cloudmindspartners.com"]');
    await expect(emailLink).toBeVisible();
  });
});
```

**tests/phase3-collections.spec.ts** (covers CONT-02..06):
```typescript
import { test, expect } from '@playwright/test';

test.describe('Phase 3 collection routes @smoke', () => {
  test('CONT-02: memos index returns 200', async ({ page }) => {
    const res = await page.goto('/intelligence/memos/');
    expect(res?.status()).toBe(200);
    // Either memo list OR empty state — both are valid
    const body = await page.textContent('body');
    expect(body).toBeTruthy();
  });

  test('CONT-02: memos empty state text present when no memos', async ({ page }) => {
    await page.goto('/intelligence/memos/');
    const body = await page.textContent('body');
    // If no memos published, empty state must show
    const hasMemoList = await page.locator('.memo-list').count() > 0;
    if (!hasMemoList) {
      expect(body).toContain('Memo library expanding');
    }
  });

  test('CONT-04: radar index returns 200, shows 1+ entries after gate flip', async ({ page }) => {
    const res = await page.goto('/intelligence/radar/');
    expect(res?.status()).toBe(200);
    const body = await page.textContent('body');
    expect(body).toContain('2026-W20');
  });

  test('CONT-05: radar detail /intelligence/radar/2026-W20 returns 200', async ({ page }) => {
    const res = await page.goto('/intelligence/radar/2026-W20');
    expect(res?.status()).toBe(200);
    const body = await page.textContent('body');
    expect(body).toContain('2026-W20');
  });

  test('CONT-06: regwatch index returns 200', async ({ page }) => {
    const res = await page.goto('/intelligence/regwatch/');
    expect(res?.status()).toBe(200);
    const body = await page.textContent('body');
    // Empty state
    expect(body).toContain('Regulatory watch');
  });
});
```

**tests/phase3-i18n.spec.ts** (covers I18N-03..05):
```typescript
import { test, expect } from '@playwright/test';

test.describe('Phase 3 i18n @smoke', () => {
  test('I18N-04: hreflang alternates present on home', async ({ page }) => {
    await page.goto('/');
    const hreflangEn = page.locator('link[hreflang="en"]');
    await expect(hreflangEn).toHaveCount(1);
    const hreflangPt = page.locator('link[hreflang="pt-BR"]');
    await expect(hreflangPt).toHaveCount(1);
    const hreflangEs = page.locator('link[hreflang="es"]');
    await expect(hreflangEs).toHaveCount(1);
  });

  test('I18N-04: hreflang present on advisory', async ({ page }) => {
    await page.goto('/advisory');
    await expect(page.locator('link[hreflang="en"]')).toHaveCount(1);
  });

  test('I18N-04: hreflang present on memos index', async ({ page }) => {
    await page.goto('/intelligence/memos/');
    await expect(page.locator('link[hreflang="en"]')).toHaveCount(1);
  });

  test('I18N-05: /pt/ renders PT headline (not EN)', async ({ page }) => {
    await page.goto('/pt/');
    const h1 = page.locator('h1').first();
    // PT home headline must NOT be the EN string
    await expect(h1).not.toHaveText('The LatAm Data Center Intelligence Firm');
    // Must contain Portuguese text
    const text = await h1.textContent();
    expect(text).toBeTruthy();
  });

  test('I18N-05: /es/ renders ES headline (not EN)', async ({ page }) => {
    await page.goto('/es/');
    const h1 = page.locator('h1').first();
    await expect(h1).not.toHaveText('The LatAm Data Center Intelligence Firm');
  });

  test('I18N-03: LangSwitcher on /team links to /pt/team', async ({ page }) => {
    await page.goto('/team');
    const ptLink = page.locator('.lang-link[href="/pt/team"], a[href="/pt/team"]').first();
    await expect(ptLink).toBeVisible();
  });

  test('I18N-03: /pt/team returns 200 (Astro rewrite fallback)', async ({ page }) => {
    const res = await page.goto('/pt/team');
    expect(res?.status()).toBe(200);
  });
});
```
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck 2>/dev/null; ls /Users/edgardabreu/Projects/CODE/cmp-website/tests/phase3-pages.spec.ts /Users/edgardabreu/Projects/CODE/cmp-website/tests/phase3-collections.spec.ts /Users/edgardabreu/Projects/CODE/cmp-website/tests/phase3-i18n.spec.ts</automated>
  </verify>
  <done>3 spec files exist; TypeScript compiles without errors on the spec files themselves.</done>
</task>

<task type="auto">
  <name>Task 2: Delete scaffolding, wire platforms redirect</name>
  <files>src/pages/image-pipeline-test.astro, src/pages/newsletter.astro, src/pages/platforms/[slug].astro</files>
  <action>
**Delete src/pages/image-pipeline-test.astro** — Phase 1 scaffolding, no longer needed. Real pages now import photos (FOUND-01 WebP/srcset gates stay green via real page builds).

**Delete src/pages/newsletter.astro** — Newsletter is inline in the home page per locked CONTEXT.md decision. Deleting avoids a dead /newsletter route.

**Replace src/pages/platforms/[slug].astro** with a redirect-only file:
```astro
---
export function getStaticPaths() {
  return [
    { params: { slug: 'site-selection' } },
    { params: { slug: 'grid-intelligence' } },
    { params: { slug: 'dc-financial' } },
    { params: { slug: 'test-fit-pro' } },
  ];
}
const { slug } = Astro.params;
return Astro.redirect(`/platforms#platforms-${slug}`, 301);
---
```

Note: In Astro SSG, `Astro.redirect()` in a page with `getStaticPaths()` works at build time — Astro generates an HTML file with `<meta http-equiv="refresh">` pointing to the anchor. This is the standard SSG redirect pattern for static deploys.
  </action>
  <verify>
    <automated>test ! -f /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/image-pipeline-test.astro && test ! -f /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/newsletter.astro && grep -q "redirect" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/platforms/\[slug\].astro</automated>
  </verify>
  <done>image-pipeline-test.astro and newsletter.astro do not exist; platforms/[slug].astro contains Astro.redirect call.</done>
</task>

</tasks>

<verification>
1. `ls tests/phase3-*.spec.ts | wc -l` → 3
2. `test ! -f src/pages/image-pipeline-test.astro` → passes
3. `test ! -f src/pages/newsletter.astro` → passes
4. `grep redirect src/pages/platforms/\[slug\].astro` → found
5. `npx astro build 2>&1 | tail -5` → exits 0 (deletion of files doesn't break build)
</verification>

<success_criteria>
- 3 spec files created covering all 16 Phase 3 requirements
- image-pipeline-test.astro and newsletter.astro deleted
- platforms/[slug].astro returns 301 redirect
- `npx astro build` exits 0 after file deletions
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages/03-00-SUMMARY.md`
</output>
