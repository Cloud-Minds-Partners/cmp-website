---
phase: 03-pages
plan: "02"
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - src/pages/index.astro
  - src/pages/advisory.astro
  - src/pages/development.astro
  - src/pages/platforms.astro
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-05

must_haves:
  truths:
    - "Home page renders 3-slide hero rotation with SP/Santiago/CDMX photos"
    - "Home page has stats section (76 cities / 251 sites / 508 studies / +18 GW)"
    - "Home page has 4 CapabilityCards in the platforms section (cream bg, on-cream variant)"
    - "Home page has principles section (3 principles) and insight/featured-memo section"
    - "Home page has NewsletterSubscribe section as last section before footer"
    - "Advisory page renders 6 CapabilityCards matching the 6 advisory lenses"
    - "Development page renders 3 role cards in a roles-grid"
    - "Platforms page renders 4 products in alternating split layout"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Home page — complete mock-26 section composition"
      min_lines: 120
    - path: "src/pages/advisory.astro"
      provides: "Advisory page — hero + figures strip + 6 cap-cards + engagement model + CTA bar"
      min_lines: 100
    - path: "src/pages/development.astro"
      provides: "Development page — hero + roles grid + lifecycle strip"
      min_lines: 80
    - path: "src/pages/platforms.astro"
      provides: "Platforms page — hero + 4-product alternating split layout"
      min_lines: 120
  key_links:
    - from: "src/pages/index.astro"
      to: "src/assets/photos/hero-sp-marginal.jpg"
      via: "import heroSp from '../assets/photos/hero-sp-marginal.jpg'"
      pattern: "import heroSp"
    - from: "src/pages/index.astro"
      to: "src/components/NewsletterSubscribe.astro"
      via: "import NewsletterSubscribe"
      pattern: "NewsletterSubscribe"
    - from: "src/pages/advisory.astro"
      to: "src/components/CapabilityCard.astro"
      via: "import CapabilityCard"
      pattern: "CapabilityCard"
    - from: "src/pages/platforms.astro"
      to: "src/assets/photos/card-platform-*.jpg"
      via: "import cardPlatformXxx"
      pattern: "card-platform"
---

<objective>
Build the four marketing pages (home, advisory, development, platforms) as pixel-faithful compositions of mock-26, using Phase 2 components and i18n keys.

Purpose: These are the highest-traffic pages and form the core commercial presence of the site.
Output: 4 fully-composed .astro page files replacing v4 Crusoe content.
</objective>

<execution_context>
@/Users/edgardabreu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edgardabreu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-pages/03-RESEARCH.md
@.planning/phases/03-pages/03-CONTEXT.md

<interfaces>
<!-- Hero.astro Props -->
interface Props {
  variant: 'home' | 'page';
  slides?: { src: ImageMetadata; alt: string }[];  // home only
  image?: ImageMetadata | string;                   // page only
  eyebrow?: string;
  live?: boolean;
  heading: string;
  subheading?: string;
  actions?: { label: string; href: string; variant: 'primary' | 'outline' }[];
}

<!-- CapabilityCard.astro Props -->
interface Props {
  num: string;         // "01 — Site"
  title: string;
  description: string;
  photo: ImageMetadata;  // MUST be imported from src/assets/photos/
  href?: string;
  onCream?: boolean;
  class?: string;
}

<!-- SEO.astro Props -->
interface Props {
  title: string;
  description: string;
  image?: string;
  canonical?: string;
  lang?: string;
  type?: 'website' | 'article';
  noindex?: boolean;
}

<!-- NewsletterSubscribe.astro: no required props (self-contained) -->

<!-- Page scaffold pattern -->
import Base from '../layouts/Base.astro';
import SEO from '../components/SEO.astro';
import SiteHeader from '../components/SiteHeader.astro';
import SiteFooter from '../components/SiteFooter.astro';
import Hero from '../components/Hero.astro';
import { useTranslations } from '../i18n/utils';
const locale = Astro.currentLocale ?? 'en';
const t = useTranslations(locale as 'en' | 'pt' | 'es');

<!-- Photo filenames (from INVENTORY.md — all in src/assets/photos/) -->
Home hero rotation: hero-sp-marginal.jpg, hero-santiago.jpg, hero-cdmx.jpg
Home cap-grid cards: card-home-site-selection.jpg, card-home-grid-intel.jpg, card-home-dc-financial.jpg, card-home-testfit-pro.jpg
Home featured memo: memo-home-grid-headroom.jpg
Advisory hero: hero-advisory.jpg
Advisory cap-cards: card-adv-site-selection.jpg, card-adv-power-grid.jpg, card-adv-water-climate.jpg, card-adv-regulatory.jpg, card-adv-financial.jpg, card-adv-market-intel.jpg
Development hero: hero-development.jpg
Development role cards: card-dev-codeveloper.jpg, card-dev-tech-advisor.jpg, card-dev-financial-partner.jpg
Platforms hero: hero-platforms.jpg
Platforms product panels: card-platform-site-selection.jpg, card-platform-grid-intel.jpg, card-platform-dc-financial.jpg, card-platform-testfit-pro.jpg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home page (index.astro) — mock-26 verbatim section order</name>
  <files>src/pages/index.astro</files>
  <action>
REPLACE ALL content of src/pages/index.astro. Follow the mock-26 section order VERBATIM (from RESEARCH.md §Home section map):
1. Hero (3-rotation)
2. Stats section
3. Capabilities/Platforms section (cream bg, 4 CapabilityCards with onCream)
4. Principles section (cream bg, 3 principles)
5. Insight/Featured-memo section (near-black bg)
6. Newsletter section
7. Footer

**Locked decisions:**
- No team teaser (mock-26 has none; omit per planning context)
- Newsletter section IS present (locked CONTEXT.md decision) — place AFTER insight, BEFORE footer

**Section implementation:**

```astro
---
import Base from '../layouts/Base.astro';
import SEO from '../components/SEO.astro';
import SiteHeader from '../components/SiteHeader.astro';
import SiteFooter from '../components/SiteFooter.astro';
import Hero from '../components/Hero.astro';
import CapabilityCard from '../components/CapabilityCard.astro';
import NewsletterSubscribe from '../components/NewsletterSubscribe.astro';
import { useTranslations } from '../i18n/utils';

import heroSp from '../assets/photos/hero-sp-marginal.jpg';
import heroSantiago from '../assets/photos/hero-santiago.jpg';
import heroCdmx from '../assets/photos/hero-cdmx.jpg';
import cardSiteSelection from '../assets/photos/card-home-site-selection.jpg';
import cardGridIntel from '../assets/photos/card-home-grid-intel.jpg';
import cardDcFinancial from '../assets/photos/card-home-dc-financial.jpg';
import cardTestFit from '../assets/photos/card-home-testfit-pro.jpg';
import memoImg from '../assets/photos/memo-home-grid-headroom.jpg';
import { Image } from 'astro:assets';

const locale = Astro.currentLocale ?? 'en';
const t = useTranslations(locale as 'en' | 'pt' | 'es');
---

<Base lang={locale}>
  <SEO slot="head"
    title="Cloud Minds Partners — The LatAm DC Intelligence Firm"
    description={t('home.hero.subhead')}
    lang={locale}
  />
  <SiteHeader />

  <!-- 1. HERO: 3-city rotation -->
  <Hero
    variant="home"
    slides={[
      { src: heroSp, alt: 'São Paulo skyline — Marginal Pinheiros' },
      { src: heroSantiago, alt: 'Santiago skyline with Cordillera backdrop' },
      { src: heroCdmx, alt: 'Mexico City — Paseo de la Reforma' },
    ]}
    heading={t('home.hero.headline')}
    subheading={t('home.hero.subhead')}
    actions={[
      { label: t('home.hero.cta-primary'), href: 'mailto:info@cloudmindspartners.com', variant: 'primary' },
      { label: t('home.hero.cta-secondary'), href: '/platforms', variant: 'outline' },
    ]}
  />

  <!-- 2. STATS: near-black bg, 4-column grid -->
  <section class="stats">
    <div class="stats-inner">
      <div class="stat-item">
        <span class="stat-value">{t('home.stat.cities.value')}</span>
        <span class="stat-label">{t('home.stat.cities.label')}</span>
      </div>
      <div class="stat-item">
        <span class="stat-value">{t('home.stat.sites.value')}</span>
        <span class="stat-label">{t('home.stat.sites.label')}</span>
      </div>
      <div class="stat-item">
        <span class="stat-value">{t('home.stat.studies.value')}</span>
        <span class="stat-label">{t('home.stat.studies.label')}</span>
      </div>
      <div class="stat-item">
        <span class="stat-value">{t('home.stat.headroom.value')}</span>
        <span class="stat-label">{t('home.stat.headroom.label')}</span>
      </div>
    </div>
  </section>

  <!-- 3. CAPABILITIES / PLATFORMS: cream bg, 2x2 CapabilityCard grid -->
  <section class="capabilities on-cream" id="platforms">
    <div class="section-inner">
      <p class="section-eyebrow">{t('home.capabilities.eyebrow')}</p>
      <h2>{t('home.capabilities.heading')}</h2>
      <p class="section-subhead">{t('home.capabilities.subhead')}</p>
      <p class="section-subhead-detail">{t('home.capabilities.subhead-detail')}</p>
      <div class="cap-grid">
        <CapabilityCard onCream
          num={t('home.card.site-selection.num')}
          title={t('home.card.site-selection.title')}
          description={t('home.card.site-selection.desc')}
          photo={cardSiteSelection}
          href="/platforms"
        />
        <CapabilityCard onCream
          num={t('home.card.grid-intel.num')}
          title={t('home.card.grid-intel.title')}
          description={t('home.card.grid-intel.desc')}
          photo={cardGridIntel}
          href="/platforms"
        />
        <CapabilityCard onCream
          num={t('home.card.dc-financial.num')}
          title={t('home.card.dc-financial.title')}
          description={t('home.card.dc-financial.desc')}
          photo={cardDcFinancial}
          href="/platforms"
        />
        <CapabilityCard onCream
          num={t('home.card.testfit-pro.num')}
          title={t('home.card.testfit-pro.title')}
          description={t('home.card.testfit-pro.desc')}
          photo={cardTestFit}
          href="/platforms"
        />
      </div>
    </div>
  </section>

  <!-- 4. PRINCIPLES: cream bg, 3-col grid -->
  <section class="principles on-cream">
    <div class="section-inner">
      <p class="section-eyebrow">{t('home.principles.eyebrow')}</p>
      <h2>{t('home.principles.heading')}</h2>
      <div class="principles-grid">
        <div class="principle">
          <span class="principle-num">{t('home.principle.sources.num')}</span>
          <h3>{t('home.principle.sources.title')}</h3>
          <p>{t('home.principle.sources.desc')}</p>
        </div>
        <div class="principle">
          <span class="principle-num">{t('home.principle.partners.num')}</span>
          <h3>{t('home.principle.partners.title')}</h3>
          <p>{t('home.principle.partners.desc')}</p>
        </div>
        <div class="principle">
          <span class="principle-num">{t('home.principle.audited.num')}</span>
          <h3>{t('home.principle.audited.title')}</h3>
          <p>{t('home.principle.audited.desc')}</p>
        </div>
      </div>
    </div>
  </section>

  <!-- 5. INSIGHT / FEATURED MEMO: near-black bg -->
  <section class="insight" id="intelligence">
    <div class="insight-inner">
      <div class="insight-visual">
        <Image src={memoImg} alt="Grid headroom analysis visualization" widths={[640, 960]} sizes="(max-width: 768px) 100vw, 50vw" format="webp" class="insight-img" />
        <div class="insight-stat-overlay">
          <span class="stat-num">+18 GW</span>
          <span class="stat-ctx">Headroom 2028</span>
        </div>
      </div>
      <div class="insight-text">
        <p class="section-eyebrow">{t('home.insight.eyebrow')}</p>
        <h2>Headroom in Latin America's Power Grid — A First Look</h2>
        <p class="insight-meta">Cloud Minds Partners · Grid Intelligence · 2026</p>
        <p class="insight-desc">Brazil, Mexico, and Chile have confirmed +18 GW of N-1 headroom in transmission substations near major data center corridors through 2028, based on ONS PARPEL 2027–2031 data.</p>
        <a href="/intelligence" class="btn-insight">{t('home.insight.cta')} →</a>
      </div>
    </div>
  </section>

  <!-- 6. NEWSLETTER -->
  <section class="newsletter-section">
    <div class="section-inner">
      <h2>{t('home.newsletter.heading')}</h2>
      <p>{t('home.newsletter.subhead')}</p>
      <NewsletterSubscribe />
    </div>
  </section>

  <SiteFooter />
</Base>
```

**CRITICAL: Insight section content** — The featured memo text is REAL CMP intelligence data (from the +18 GW grid headroom memo). Do NOT invent details. The h2 and description above are from actual CMP research (PARPEL 2027-2031 data confirmed in cmp-knowledge). If more accurate headline is available, use it.

**NOTE: Insight section is EN-hardcoded in all locales** — The h2, insight-meta, and insight-desc are intentionally hardcoded EN strings (not behind t()). This is per ROADMAP criterion #4 exemption: the insight section references a specific published English memo with concrete claims. PT/ES localization of this section is deferred to v2. The `t('home.insight.eyebrow')` and `t('home.insight.cta')` CTA label ARE translated; only the memo body copy stays EN.

Add a `<style>` block with mock-26-faithful CSS for sections:
- `.stats` — `background: rgba(5,14,29,0.97)`, padding `clamp(48px,5vw,80px) 0`, `.stats-inner` grid 4 columns, `.stat-value` large numeral, `.stat-label` small label
- `.capabilities.on-cream` — `background: var(--color-cream-bg, #f5f0e8)`, `.cap-grid` CSS grid `repeat(2, 1fr)` with 24px gap, responsive to 1 col below 768px
- `.principles.on-cream` — cream bg, `.principles-grid` 3-col, each principle has num + h3 + p
- `.insight` — `background: #0a0e18`, `.insight-inner` 2-col grid (image left, text right), `.insight-visual` position:relative for stat overlay
- `.newsletter-section` — navy bg, centered, wraps `<NewsletterSubscribe />`
- Section padding: `clamp(64px,7vw,112px) clamp(28px,4vw,64px)` (matching mock-26 rhythm)
  </action>
  <verify>
    <automated>grep -c "slide" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/index.astro && grep "NewsletterSubscribe" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/index.astro && grep "cap-grid" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/index.astro</automated>
  </verify>
  <done>index.astro has Hero with 3 slides, CapabilityCard grid, principles, insight, and NewsletterSubscribe — all sections present, file > 120 lines. Insight section hardcoded EN per ROADMAP #4 exemption (references specific published EN memo).</done>
</task>

<task type="auto">
  <name>Task 2: Advisory, Development, and Platforms pages</name>
  <files>src/pages/advisory.astro, src/pages/development.astro, src/pages/platforms.astro</files>
  <action>
REPLACE ALL content of each file. Each page follows the page scaffold pattern.

**advisory.astro** — Mock-26 section order (5 sections + footer):
1. Hero (variant="page", hero-advisory.jpg, eyebrow "Advisory")
2. Figures strip (near-black, 4-col: "76 Cities / 30+ Engagements / 1B+ USD informed / 3 Markets")
3. Capabilities section (cream bg, "Six lenses. One picture.", 2x3 CapabilityCard grid — onCream=false since the mock uses dark cards on cream background; check mock — actually use onCream=true to match mock-26's `.on-cream` parent section)
4. Engagement model (navy bg, 4-step timeline: Thesis 3-4wk / Shortlist 6-10wk / Diligence 4-8wk / Close Variable)
5. CTA bar (cream, h2 "Bring a thesis. We'll pressure-test it.", 2 buttons: "Engage advisory" → mailto, "Read a memo" → /intelligence/memos)

CapabilityCard photos for advisory (6 cards):
- card-adv-site-selection.jpg → "01" / "Site selection" / "Urban land parcels, zoning, access roads."
- card-adv-power-grid.jpg → "02" / "Power & grid" / "Grid capacity, tariff regime, substation headroom."
- card-adv-water-climate.jpg → "03" / "Water & climate" / "WUE constraints, climate risk, flooding."
- card-adv-regulatory.jpg → "04" / "Regulatory & tax" / "Permits, incentives, fiscal structure."
- card-adv-financial.jpg → "05" / "Financial structuring" / "Equity stack, IRR, FX exposure."
- card-adv-market-intel.jpg → "06" / "Market intelligence" / "Demand pipeline, competitive landscape."

Use scoped `<style>` for: `.figures-strip` 4-col grid near-black, `.engagement-timeline` 4-step horizontal timeline on navy, `.adv-cta` cream CTA bar.

SEO: `title="Advisory — Cloud Minds Partners"`, `description="LatAm data center advisory — site selection, power & grid, financial structuring. Six lenses. One picture."`.

---

**development.astro** — Mock-26 section order (3 sections + footer):
1. Hero (variant="page", hero-development.jpg, eyebrow "Development")
2. Roles grid (cream bg, "Three ways we sit on the cap table.", 3 role cards)
3. Lifecycle strip (navy bg, "Kickoff to commissioning.", 4-phase horizontal track)

Role cards use a custom `.role-card` pattern (NOT CapabilityCard — per RESEARCH.md implementation note). Build inline:
```astro
<div class="roles-grid">
  <div class="role-card">
    <Image src={cardCodeveloper} alt="Co-developer — equity participation" ... />
    <div class="role-card-body">
      <span class="role-num">01</span>
      <h3>Co-developer</h3>
      <p class="role-mode">Equity</p>
      <p>We take a seat at the table — aligned incentives from day one through commissioning.</p>
    </div>
  </div>
  <!-- repeat for Tech advisor (02-Fee-based) and Financial partner (03-Capital) -->
</div>
```

Photos: card-dev-codeveloper.jpg, card-dev-tech-advisor.jpg, card-dev-financial-partner.jpg.

Lifecycle: 4-phase `.lifecycle-track` strip with phases Pre-site / Engineering & permits / Construction / Operate & iterate.

SEO: `title="Development — Cloud Minds Partners"`, description about co-development in LatAm.

---

**platforms.astro** — Mock-26 section order (2 sections + footer):
1. Hero (variant="page", hero-platforms.jpg, eyebrow "Platforms")
2. Products: 4 products in alternating split layout

Per RESEARCH.md: The `.product` alternating layout is unique — NOT CapabilityCard. Build with scoped CSS:
```astro
<section class="products">
  <div class="product product--1" id="platforms-site-selection">
    <div class="product-info">
      <span class="product-num">01</span>
      <h2>Site Selection</h2>
      <p>Score 76+ LatAm cities by land cost, grid capacity, water availability, and fiscal regime. Data-driven shortlists in 48 hours.</p>
      <a href="mailto:info@cloudmindspartners.com" class="btn-platform">Request access</a>
    </div>
    <div class="product-visual">
      <Image src={cardPlatformSiteSelection} alt="Site Selection platform — city scoring interface" ... />
    </div>
  </div>
  <!-- product--2 (grid-intel): photo-left (order swap via CSS even selector) -->
  <!-- product--3 (dc-financial): info-left -->
  <!-- product--4 (testfit-pro): photo-left -->
</section>
```

CSS: `.product` uses CSS grid `1fr 1fr`, alternating dark/cream per product. Even products: `.product-visual` gets `order: -1`. Each product has `id="platforms-<slug>"` matching the redirect from platforms/[slug].astro.

Products (4):
- 01 Site Selection — card-platform-site-selection.jpg — navy bg — id="platforms-site-selection"
- 02 Grid Intelligence — card-platform-grid-intel.jpg — cream bg — id="platforms-grid-intelligence"
- 03 DC Financial Model — card-platform-dc-financial.jpg — navy bg — id="platforms-dc-financial"
- 04 Test Fit Pro — card-platform-testfit-pro.jpg — cream bg — id="platforms-test-fit-pro"

SEO: `title="Platforms — Cloud Minds Partners"`, description about 4 purpose-built platforms.

**PATTERN NOTE for all pages:** Every page must have SiteHeader + SiteFooter wrapping content inside `<Base lang={locale}>`. Every page must use `<SEO slot="head" ... />`. No hardcoded English strings in templates — use `t()` for all UI copy. Hardcoded content in advisory/development/platforms (specific product descriptions, figure values, engagement phases) is acceptable since it is EN-only in Phase 3 (non-home pages fall back to EN per locked scope decision I18N-05).
  </action>
  <verify>
    <automated>grep -c "CapabilityCard" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/advisory.astro && grep -c "role-card" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/development.astro && grep -c "product" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/platforms.astro && grep "SEO" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/advisory.astro</automated>
  </verify>
  <done>advisory.astro has 6 CapabilityCards; development.astro has 3 role-card elements; platforms.astro has 4 product sections; all 3 pages have SEO slot.</done>
</task>

</tasks>

<verification>
1. `npx astro build 2>&1 | grep -i error` → no errors
2. `grep -c "slide" src/pages/index.astro` → 3+ (hero slides array)
3. `grep "NewsletterSubscribe" src/pages/index.astro` → found
4. `grep -c "cap-card" src/pages/advisory.astro` → confirms 6 cards
5. `grep -c "role-card" src/pages/development.astro` → 3
6. `grep -c "product--" src/pages/platforms.astro` → 4
7. `grep "SEO slot" src/pages/index.astro src/pages/advisory.astro src/pages/development.astro src/pages/platforms.astro` → all 4 found
</verification>

<success_criteria>
- Home: 3-slide Hero rotation, stats (4 stats), 4 CapabilityCards, principles (3), insight, NewsletterSubscribe — all present
- Advisory: Hero + figures strip + 6 CapabilityCards + engagement model + CTA bar
- Development: Hero + 3 role-cards + lifecycle strip
- Platforms: Hero + 4 products alternating split layout with anchor IDs
- All 4 pages: SEO slot, locale-aware, npx astro build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages/03-02-SUMMARY.md`
</output>
