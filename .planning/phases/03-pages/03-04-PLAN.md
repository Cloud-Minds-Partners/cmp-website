---
phase: 03-pages
plan: "04"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/pages/intelligence/index.astro
  - src/pages/intelligence/memos/index.astro
  - src/pages/intelligence/memos/[slug].astro
  - src/pages/intelligence/radar/index.astro
  - src/pages/intelligence/radar/[week].astro
  - src/pages/intelligence/regwatch/index.astro
  - src/pages/intelligence/regwatch/[month].astro
autonomous: true
requirements:
  - PAGE-04
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CONT-07

must_haves:
  truths:
    - "/intelligence renders 200 with featured memo section and 3 subsection links"
    - "/intelligence/memos/ renders 200 and shows empty-state text when no memos published"
    - "/intelligence/radar/ renders 200 and shows 2026-W20 entry (after gate flip in 03-01)"
    - "/intelligence/radar/2026-W20 renders 200 via RadarLayout"
    - "/intelligence/regwatch/ renders 200 and shows empty-state text"
    - "All collection index pages use locale-aware getCollection filtering + LocaleBanner when EN fallback"
    - "Memo detail page uses MemoLayout (updated in 03-01 with SEO slot + locale-aware back link)"
  artifacts:
    - path: "src/pages/intelligence/index.astro"
      provides: "Intelligence hub — hero + streams subnav + featured memo + memos archive list"
      min_lines: 80
    - path: "src/pages/intelligence/memos/index.astro"
      provides: "Memos index with locale filter + empty state"
      min_lines: 50
    - path: "src/pages/intelligence/memos/[slug].astro"
      provides: "Memo detail via MemoLayout"
      min_lines: 20
    - path: "src/pages/intelligence/radar/index.astro"
      provides: "Radar index showing 2026-W20"
      min_lines: 50
    - path: "src/pages/intelligence/radar/[week].astro"
      provides: "Radar detail via RadarLayout + getStaticPaths"
      min_lines: 30
    - path: "src/pages/intelligence/regwatch/index.astro"
      provides: "Regwatch index with empty state"
      min_lines: 40
    - path: "src/pages/intelligence/regwatch/[month].astro"
      provides: "Regwatch detail via RegwatchLayout + getStaticPaths"
      min_lines: 20
  key_links:
    - from: "src/pages/intelligence/index.astro"
      to: "src/components/LocaleBanner.astro"
      via: "import LocaleBanner"
      pattern: "LocaleBanner"
    - from: "src/pages/intelligence/radar/index.astro"
      to: "cmp-knowledge/innovation-radar/2026-W20.md"
      via: "getCollection('radar', r => r.data.published)"
      pattern: "getCollection.*radar"
    - from: "src/pages/intelligence/radar/[week].astro"
      to: "src/layouts/RadarLayout.astro"
      via: "import RadarLayout"
      pattern: "RadarLayout"
    - from: "src/pages/intelligence/memos/[slug].astro"
      to: "src/layouts/MemoLayout.astro"
      via: "import MemoLayout"
      pattern: "MemoLayout"
---

<objective>
Build the intelligence hub and all 6 content collection routes (memos, radar, regwatch — index + detail).

Purpose: PAGE-04 (Intelligence hub) and CONT-02..07 (all collection routes). These wire the existing RadarLayout/MemoLayout/RegwatchLayout (upgraded in 03-01) to real getCollection data.
Output: 7 page files handling both populated (radar/2026-W20) and empty (memos, regwatch) collection states.
</objective>

<execution_context>
@/Users/edgardabreu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edgardabreu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-pages/03-RESEARCH.md

<interfaces>
<!-- getCollection pattern with language filter and empty state -->
const locale = Astro.currentLocale ?? 'en';
const t = useTranslations(locale as 'en' | 'pt' | 'es');

// For published-gated collections (radar, regwatch):
const allEntries = await getCollection('radar', (r) => r.data.published);
const localeEntries = allEntries.filter(r => r.data.language === locale);
const entries = (localeEntries.length > 0 ? localeEntries : allEntries)
  .sort((a, b) => (b.data.period_start?.valueOf() ?? 0) - (a.data.period_start?.valueOf() ?? 0));
const showFallbackBanner = localeEntries.length === 0 && locale !== 'en';

// For memos (gated by publish boolean field):
const allMemos = await getCollection('memos', (m) => m.data.publish);

// Radar getStaticPaths:
export async function getStaticPaths() {
  const entries = await getCollection('radar', (r) => r.data.published);
  return entries.map(entry => ({
    params: { week: entry.data.week ?? entry.id },
    props: { entry },
  }));
}

// Memo getStaticPaths:
export async function getStaticPaths() {
  const entries = await getCollection('memos', (m) => m.data.publish);
  return entries.map(entry => ({
    params: { slug: entry.data.slug },
    props: { entry },
  }));
}

<!-- RadarLayout Props (updated in 03-01) -->
interface Props {
  week: string;
  period_start: Date;
  period_end: Date;
  author: string;
  description?: string;
  title?: string;  // after 03-01 update adds SEO slot
}

<!-- MemoLayout Props (updated in 03-01) -->
interface Props {
  title: string;
  description?: string;
  jurisdiction: { country: string; scope: string; region?: string };
  author: string;
  created: Date;
  updated: Date;
  word_count?: number;
  sources_count?: number;
  tags?: string[];
}

<!-- LocaleBanner: no props, reads Astro.currentLocale internally -->
import LocaleBanner from '../../components/LocaleBanner.astro';

<!-- i18n empty state keys (exist in all 3 JSON files from 03-01 work) -->
t('common.empty-memos')    → "Memo library expanding — first publication coming soon."
t('common.empty-regwatch') → "Regulatory watch — first digest coming soon."

<!-- hero-intelligence.jpg is the correct hero for intelligence pages -->
import heroIntelligence from '../../assets/photos/hero-intelligence.jpg';
import memoFeaturedImg from '../../assets/photos/memo-intelligence-grid.jpg';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Intelligence hub (intelligence/index.astro)</name>
  <files>src/pages/intelligence/index.astro</files>
  <action>
REPLACE ALL content of src/pages/intelligence/index.astro. Follow mock-26 intelligence.html section order:
1. Hero (hero-intelligence.jpg, eyebrow "Intelligence")
2. Streams subnav tabs linking to collection index pages (NOT anchors)
3. Featured memo section (cream bg, 2-col: image left + text right)
4. Memos archive list (navy bg — renders from getCollection('memos') filtered + sorted; shows empty state when 0)
5. Footer

```astro
---
import Base from '../../layouts/Base.astro';
import SEO from '../../components/SEO.astro';
import SiteHeader from '../../components/SiteHeader.astro';
import SiteFooter from '../../components/SiteFooter.astro';
import Hero from '../../components/Hero.astro';
import LocaleBanner from '../../components/LocaleBanner.astro';
import { getCollection } from 'astro:content';
import { useTranslations } from '../../i18n/utils';
import { Image } from 'astro:assets';

import heroIntelligence from '../../assets/photos/hero-intelligence.jpg';
import memoFeaturedImg from '../../assets/photos/memo-intelligence-grid.jpg';

const locale = Astro.currentLocale ?? 'en';
const t = useTranslations(locale as 'en' | 'pt' | 'es');

const allMemos = await getCollection('memos', (m) => m.data.publish);
const localeMemos = allMemos.filter(m => m.data.language === locale);
const memos = (localeMemos.length > 0 ? localeMemos : allMemos)
  .sort((a, b) => b.data.created.valueOf() - a.data.created.valueOf());
const showFallbackBanner = localeMemos.length === 0 && locale !== 'en';

// Subnav: streams tabs linking to collection index pages
const streams = [
  { label: 'Technical Memos', href: '/intelligence/memos', count: allMemos.length },
  { label: 'Innovation Radar', href: '/intelligence/radar', count: null },
  { label: 'Regulatory Watch', href: '/intelligence/regwatch', count: null },
];
---

<Base lang={locale}>
  <SEO slot="head"
    title="Intelligence — Cloud Minds Partners"
    description="Technical memos, innovation radar, and regulatory watch covering LatAm data center markets."
    lang={locale}
  />
  <SiteHeader
    subnav={streams.map(s => ({ href: s.href, label: s.label }))}
  />

  {showFallbackBanner && <LocaleBanner />}

  <Hero
    variant="page"
    image={heroIntelligence}
    eyebrow="Intelligence"
    heading="Primary-source intelligence on LatAm data centers."
    subheading="Technical memos, grid radar, and regulatory watch — audited before delivery."
  />

  <!-- Featured memo (cream bg, 2-col grid) -->
  <section class="featured on-cream" id="memos">
    <div class="section-inner">
      <div class="featured-grid">
        <div class="featured-visual">
          <Image
            src={memoFeaturedImg}
            alt="Grid intelligence visualization"
            widths={[640, 960]}
            sizes="(max-width: 768px) 100vw, 50vw"
            format="webp"
            class="featured-img"
          />
        </div>
        <div class="featured-text">
          <p class="section-eyebrow">Latest memo</p>
          <h2>Headroom in Latin America's Power Grid</h2>
          <p class="featured-meta">Cloud Minds Partners · Grid Intelligence · May 2026</p>
          <p>Brazil, Mexico, and Chile confirm +18 GW of N-1 headroom in transmission substations near major data center corridors through 2028, based on ONS PARPEL 2027–2031.</p>
          <a href="/intelligence/memos" class="btn-read">Read memos →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Memos archive (navy bg) -->
  <section class="memos-archive" id="archive">
    <div class="section-inner">
      {memos.length === 0 ? (
        <div class="empty-state">
          <p>{t('common.empty-memos')}</p>
        </div>
      ) : (
        <div class="memo-list">
          {memos.map((memo, i) => (
            <a href={`/intelligence/memos/${memo.data.slug}`} class="memo-row">
              <span class="memo-num">{String(i + 1).padStart(2, '0')}</span>
              <span class="memo-date">{memo.data.created.toLocaleDateString('en-US', { year: 'numeric', month: 'short' })}</span>
              <span class="memo-title">{memo.data.title}</span>
              <span class="memo-tag">{memo.data.topic}</span>
            </a>
          ))}
        </div>
      )}
    </div>
  </section>

  <SiteFooter />
</Base>
```

Add scoped `<style>` for:
- `.featured.on-cream` — cream bg, `.featured-grid` 2-col 1fr/1fr, responsive to 1-col below 768px
- `.featured-img` — 100% width, aspect-ratio 16/10, object-fit cover, border-radius 12px
- `.memos-archive` — `background: var(--color-navy-0)` (near-black), `.memo-list` flex column
- `.memo-row` — flex row, border-bottom rule, 4 columns: num / date / title / tag
- `.empty-state` — centered, max-width 400px, cream text, border 1px dashed
  </action>
  <verify>
    <automated>grep "getCollection" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/intelligence/index.astro && grep "empty-state\|common.empty-memos" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/intelligence/index.astro && grep "LocaleBanner" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/intelligence/index.astro</automated>
  </verify>
  <done>intelligence/index.astro has getCollection for memos, empty state, LocaleBanner, and streams subnav.</done>
</task>

<task type="auto">
  <name>Task 2a: Memos collection routes (memos/index.astro + memos/[slug].astro)</name>
  <files>src/pages/intelligence/memos/index.astro, src/pages/intelligence/memos/[slug].astro</files>
  <action>
REPLACE ALL content of both memo route files.

---

**memos/index.astro** — locale-filtered memo list:
```astro
---
import Base from '../../../layouts/Base.astro';
import SEO from '../../../components/SEO.astro';
import SiteHeader from '../../../components/SiteHeader.astro';
import SiteFooter from '../../../components/SiteFooter.astro';
import LocaleBanner from '../../../components/LocaleBanner.astro';
import { getCollection } from 'astro:content';
import { useTranslations } from '../../../i18n/utils';

const locale = Astro.currentLocale ?? 'en';
const t = useTranslations(locale as 'en' | 'pt' | 'es');

const allMemos = await getCollection('memos', (m) => m.data.publish);
const localeMemos = allMemos.filter(m => m.data.language === locale);
const memos = (localeMemos.length > 0 ? localeMemos : allMemos)
  .sort((a, b) => b.data.created.valueOf() - a.data.created.valueOf());
const showFallbackBanner = localeMemos.length === 0 && locale !== 'en';
---
<Base lang={locale}>
  <SEO slot="head" title="Technical Memos — Cloud Minds Partners" description="Primary-source technical memos on LatAm data center markets." lang={locale} />
  <SiteHeader subnav={[
    { href: '/intelligence', label: 'Overview' },
    { href: '/intelligence/memos', label: 'Technical Memos' },
    { href: '/intelligence/radar', label: 'Innovation Radar' },
    { href: '/intelligence/regwatch', label: 'Regulatory Watch' },
  ]} />
  {showFallbackBanner && <LocaleBanner />}
  <main class="collection-main">
    <div class="collection-header">
      <h1>Technical Memos</h1>
      <p>Primary-source research, audited before publication.</p>
    </div>
    {memos.length === 0 ? (
      <div class="empty-state"><p>{t('common.empty-memos')}</p></div>
    ) : (
      <div class="memo-list">
        {memos.map(memo => (
          <a href={`/intelligence/memos/${memo.data.slug}`} class="memo-row">
            <span class="memo-date">{memo.data.created.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}</span>
            <span class="memo-title">{memo.data.title}</span>
            <span class="memo-topic">{memo.data.topic}</span>
          </a>
        ))}
      </div>
    )}
  </main>
  <SiteFooter />
</Base>
```

---

**memos/[slug].astro** — memo detail via MemoLayout (updated in 03-01):
```astro
---
import MemoLayout from '../../../layouts/MemoLayout.astro';
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
  const memos = await getCollection('memos', (m) => m.data.publish);
  return memos.map(entry => ({
    params: { slug: entry.data.slug },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
---
<MemoLayout
  title={entry.data.title}
  description={entry.data.title}
  jurisdiction={entry.data.jurisdiction}
  author={entry.data.author}
  created={entry.data.created}
  updated={entry.data.updated}
  word_count={entry.data.word_count}
  sources_count={entry.data.sources_count}
  tags={entry.data.tags}
>
  <Content />
</MemoLayout>
```

NOTE: `render(entry)` is the Astro 6 API for rendering content collection entries. If build errors occur, check if the API is `entry.render()` (Astro 5) vs `render(entry)` from `astro:content` (Astro 6). Use whichever compiles cleanly.

---

**Common CSS for collection pages** — add to a shared `<style>` block in each index file:
```css
.collection-main {
  max-width: 760px;
  margin: 0 auto;
  padding: clamp(48px, 6vw, 96px) clamp(28px, 4vw, 64px);
}
.collection-header { margin-bottom: 48px; }
.collection-header h1 {
  font-size: clamp(28px, 3.5vw, 42px);
  font-weight: 600;
  color: var(--color-cream-0);
  margin: 0 0 12px;
}
.empty-state {
  border: 1px dashed rgba(250,251,253,0.15);
  border-radius: 12px;
  padding: 48px 32px;
  text-align: center;
  color: var(--color-cream-2);
  font-size: 15px;
}
.memo-row, .radar-row, .regwatch-row {
  display: flex;
  gap: 20px;
  align-items: baseline;
  padding: 16px 0;
  border-bottom: 1px solid rgba(250,251,253,0.07);
  text-decoration: none;
  color: var(--color-cream-1);
  transition: color 0.15s;
}
.memo-row:hover, .radar-row:hover, .regwatch-row:hover { color: var(--color-cream-0); }
```
  </action>
  <verify>
    <automated>grep -c "getCollection\|empty-state" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/intelligence/memos/index.astro && grep "getStaticPaths" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/intelligence/memos/\[slug\].astro && grep "MemoLayout" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/intelligence/memos/\[slug\].astro</automated>
  </verify>
  <done>memos/index.astro has getCollection + empty state + LocaleBanner; memos/[slug].astro has getStaticPaths + MemoLayout.</done>
</task>

<task type="auto">
  <name>Task 2b: Radar + Regwatch collection routes (4 files)</name>
  <files>src/pages/intelligence/radar/index.astro, src/pages/intelligence/radar/[week].astro, src/pages/intelligence/regwatch/index.astro, src/pages/intelligence/regwatch/[month].astro</files>
  <action>
REPLACE ALL content of the 4 radar and regwatch route files.

---

**radar/index.astro** — radar list (expects 1+ entry after 03-01 gate flip):
```astro
---
import Base from '../../../layouts/Base.astro';
import SEO from '../../../components/SEO.astro';
import SiteHeader from '../../../components/SiteHeader.astro';
import SiteFooter from '../../../components/SiteFooter.astro';
import LocaleBanner from '../../../components/LocaleBanner.astro';
import { getCollection } from 'astro:content';
import { useTranslations } from '../../../i18n/utils';

const locale = Astro.currentLocale ?? 'en';
const t = useTranslations(locale as 'en' | 'pt' | 'es');

const allEntries = await getCollection('radar', (r) => r.data.published);
const localeEntries = allEntries.filter(r => r.data.language === locale);
const entries = (localeEntries.length > 0 ? localeEntries : allEntries)
  .sort((a, b) => {
    const aDate = a.data.period_start?.valueOf() ?? 0;
    const bDate = b.data.period_start?.valueOf() ?? 0;
    return bDate - aDate;
  });
const showFallbackBanner = localeEntries.length === 0 && locale !== 'en';
---
<Base lang={locale}>
  <SEO slot="head" title="Innovation Radar — Cloud Minds Partners" description="Weekly innovation radar covering LatAm data center technology trends." lang={locale} />
  <SiteHeader subnav={[
    { href: '/intelligence', label: 'Overview' },
    { href: '/intelligence/memos', label: 'Technical Memos' },
    { href: '/intelligence/radar', label: 'Innovation Radar' },
    { href: '/intelligence/regwatch', label: 'Regulatory Watch' },
  ]} />
  {showFallbackBanner && <LocaleBanner />}
  <main class="collection-main">
    <div class="collection-header">
      <h1>Innovation Radar</h1>
      <p>Weekly technology and market signals for LatAm DC professionals.</p>
    </div>
    {entries.length === 0 ? (
      <div class="empty-state"><p>Innovation radar — first entry coming soon.</p></div>
    ) : (
      <div class="radar-list">
        {entries.map(entry => (
          <a href={`/intelligence/radar/${entry.data.week ?? entry.id}`} class="radar-row">
            <span class="radar-week">{entry.data.week ?? entry.id}</span>
            {entry.data.period_start && (
              <span class="radar-date">
                {entry.data.period_start.toLocaleDateString('en-US', { month: 'short', day: 'numeric' })}
                {entry.data.period_end && ` — ${entry.data.period_end.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}`}
              </span>
            )}
          </a>
        ))}
      </div>
    )}
  </main>
  <SiteFooter />
</Base>
```

---

**radar/[week].astro** — radar detail via RadarLayout:
```astro
---
import RadarLayout from '../../../layouts/RadarLayout.astro';
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
  const entries = await getCollection('radar', (r) => r.data.published);
  return entries.map(entry => ({
    params: { week: entry.data.week ?? entry.id },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
---
<RadarLayout
  week={entry.data.week ?? entry.id}
  period_start={entry.data.period_start ?? new Date(0)}
  period_end={entry.data.period_end ?? new Date(0)}
  author={entry.data.author}
  description={`Innovation Radar — ${entry.data.week ?? entry.id}`}
>
  <Content />
</RadarLayout>
```

---

**regwatch/index.astro** — regwatch list (empty collection, show empty state):
```astro
---
import Base from '../../../layouts/Base.astro';
import SEO from '../../../components/SEO.astro';
import SiteHeader from '../../../components/SiteHeader.astro';
import SiteFooter from '../../../components/SiteFooter.astro';
import LocaleBanner from '../../../components/LocaleBanner.astro';
import { getCollection } from 'astro:content';
import { useTranslations } from '../../../i18n/utils';

const locale = Astro.currentLocale ?? 'en';
const t = useTranslations(locale as 'en' | 'pt' | 'es');

const allEntries = await getCollection('regwatch', (r) => r.data.published);
const localeEntries = allEntries.filter(r => r.data.language === locale);
const entries = (localeEntries.length > 0 ? localeEntries : allEntries);
const showFallbackBanner = localeEntries.length === 0 && locale !== 'en';
---
<Base lang={locale}>
  <SEO slot="head" title="Regulatory Watch — Cloud Minds Partners" description="Monthly regulatory digest for LatAm data center markets." lang={locale} />
  <SiteHeader subnav={[
    { href: '/intelligence', label: 'Overview' },
    { href: '/intelligence/memos', label: 'Technical Memos' },
    { href: '/intelligence/radar', label: 'Innovation Radar' },
    { href: '/intelligence/regwatch', label: 'Regulatory Watch' },
  ]} />
  {showFallbackBanner && <LocaleBanner />}
  <main class="collection-main">
    <div class="collection-header">
      <h1>Regulatory Watch</h1>
      <p>Monthly digest of regulatory changes affecting LatAm data center investment.</p>
    </div>
    {entries.length === 0 ? (
      <div class="empty-state"><p>{t('common.empty-regwatch')}</p></div>
    ) : (
      <div class="regwatch-list">
        {entries.map(entry => (
          <a href={`/intelligence/regwatch/${entry.data.month ?? entry.id}`} class="regwatch-row">
            <span class="regwatch-month">{entry.data.month ?? entry.id}</span>
          </a>
        ))}
      </div>
    )}
  </main>
  <SiteFooter />
</Base>
```

---

**regwatch/[month].astro** — regwatch detail via RegwatchLayout:
```astro
---
import RegwatchLayout from '../../../layouts/RegwatchLayout.astro';
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
  const entries = await getCollection('regwatch', (r) => r.data.published);
  return entries.map(entry => ({
    params: { month: entry.data.month ?? entry.id },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
---
<RegwatchLayout
  month={entry.data.month ?? entry.id}
  author={entry.data.author}
  description={`Regulatory Watch — ${entry.data.month ?? entry.id}`}
>
  <Content />
</RegwatchLayout>
```

NOTE on regwatch/[month].astro (CONT-07): `getStaticPaths()` returns an empty array when the regwatch collection has 0 published entries — this is correct SSG behavior. Astro generates zero pages for this route, which is expected in Phase 3. The route compiles cleanly regardless of collection size.

Apply the same common CSS (`.collection-main`, `.empty-state`, `.radar-row`, `.regwatch-row`) from Task 2a to the index files here as well.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -5 && echo "CONT-07 build-proxy PASS (no published entries — accepted gap)"</automated>
  </verify>
  <done>All 4 radar + regwatch route files updated with getCollection, locale filter, empty state, and correct layout components. regwatch/[month].astro getStaticPaths gracefully returns empty array when collection has 0 entries. `npx astro build` exit 0 is the CONT-07 verification proxy since no published regwatch entries exist in Phase 3. Build success = page compiles cleanly + getStaticPaths returns empty array gracefully.</done>
</task>

</tasks>

<verification>
1. `npx astro build 2>&1 | tail -10` → exits 0
2. `grep "getCollection" src/pages/intelligence/index.astro` → found
3. `grep "common.empty-memos" src/pages/intelligence/memos/index.astro` → found
4. `grep "published" src/pages/intelligence/radar/index.astro` → found (filter by published)
5. `grep "getStaticPaths" src/pages/intelligence/radar/[week].astro` → found
6. `grep "RegwatchLayout" src/pages/intelligence/regwatch/[month].astro` → found
7. `grep "LocaleBanner" src/pages/intelligence/memos/index.astro` → found
</verification>

<success_criteria>
- intelligence/index.astro: Hero + featured memo section + memos archive (empty state when no memos)
- memos/index.astro: locale filter + empty state using t('common.empty-memos')
- memos/[slug].astro: getStaticPaths from publish-gated collection + MemoLayout
- radar/index.astro: filter by published — shows 2026-W20 after 03-01 gate flip
- radar/[week].astro: getStaticPaths + RadarLayout
- regwatch/index.astro: empty state using t('common.empty-regwatch')
- regwatch/[month].astro: getStaticPaths (0 entries = 0 static pages, compiles cleanly) + RegwatchLayout
- npx astro build exits 0 (CONT-07 build-proxy pass)
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages/03-04-SUMMARY.md`
</output>
