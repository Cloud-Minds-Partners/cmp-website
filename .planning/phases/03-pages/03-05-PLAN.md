---
phase: 03-pages
plan: "05"
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - src/pages/pt/index.astro
  - src/pages/es/index.astro
autonomous: false
requirements:
  - I18N-05

must_haves:
  truths:
    - "/pt/ serves a Portuguese home page with PT headline (not EN copy)"
    - "/es/ serves a Spanish home page with ES headline (not EN copy)"
    - "The PT and ES home pages have the same Hero 3-rotation and sections as EN home"
    - "All 15 automated Playwright tests pass"
    - "npx astro build exits 0"
  artifacts:
    - path: "src/pages/pt/index.astro"
      provides: "Portuguese home page rendering PT translations from i18n/pt.json"
      min_lines: 30
    - path: "src/pages/es/index.astro"
      provides: "Spanish home page rendering ES translations from i18n/es.json"
      min_lines: 30
  key_links:
    - from: "src/pages/pt/index.astro"
      to: "src/i18n/pt.json"
      via: "useTranslations('pt')"
      pattern: "useTranslations"
    - from: "src/pages/es/index.astro"
      to: "src/i18n/es.json"
      via: "useTranslations('es')"
      pattern: "useTranslations"
---

<objective>
Complete I18N-05 by replacing Phase 1 EN-mirror stubs for /pt/ and /es/ with locale-specific home pages. Then run the full test suite and gate with a human visual verification checkpoint.

Purpose: /pt/ and /es/ must render translated copy to satisfy I18N-05. This plan closes that requirement and verifies Phase 3 end-to-end before the phase is marked complete.
Output: pt/index.astro and es/index.astro with real translations; full Playwright suite green; visual checkpoint approved.
</objective>

<execution_context>
@/Users/edgardabreu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edgardabreu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-pages/03-VALIDATION.md

<interfaces>
<!-- Locale-specific page pattern -->
<!-- For pt/index.astro: locale = 'pt' as const; t = useTranslations('pt') -->
<!-- For es/index.astro: locale = 'es' as const; t = useTranslations('es') -->
<!-- Hardcode locale rather than using Astro.currentLocale for guaranteed correctness -->

<!-- Import paths differ: two levels up from pt/ or es/ subdirectory -->
import Base from '../../layouts/Base.astro';
import SEO from '../../components/SEO.astro';
import SiteHeader from '../../components/SiteHeader.astro';
import SiteFooter from '../../components/SiteFooter.astro';
import Hero from '../../components/Hero.astro';
import CapabilityCard from '../../components/CapabilityCard.astro';
import NewsletterSubscribe from '../../components/NewsletterSubscribe.astro';
import { useTranslations } from '../../i18n/utils';

import heroSp from '../../assets/photos/hero-sp-marginal.jpg';
import heroSantiago from '../../assets/photos/hero-santiago.jpg';
import heroCdmx from '../../assets/photos/hero-cdmx.jpg';
import cardSiteSelection from '../../assets/photos/card-home-site-selection.jpg';
import cardGridIntel from '../../assets/photos/card-home-grid-intel.jpg';
import cardDcFinancial from '../../assets/photos/card-home-dc-financial.jpg';
import cardTestFit from '../../assets/photos/card-home-testfit-pro.jpg';
import memoImg from '../../assets/photos/memo-home-grid-headroom.jpg';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PT and ES locale home pages</name>
  <files>src/pages/pt/index.astro, src/pages/es/index.astro</files>
  <action>
REPLACE ALL content of both files. Each is a copy of the EN home page (src/pages/index.astro from 03-02) with locale hardcoded.

**pt/index.astro frontmatter:**
```astro
---
// imports (same as index.astro but ../../ prefix for subdirectory depth)
const locale = 'pt' as const;
const t = useTranslations(locale);
---
```

**es/index.astro frontmatter:**
```astro
---
// imports (same as index.astro but ../../ prefix)
const locale = 'es' as const;
const t = useTranslations(locale);
---
```

**Body:** Identical sections as src/pages/index.astro:
- `<Base lang={locale}>` — lang attribute is 'pt' or 'es'
- `<SEO slot="head" title="Cloud Minds Partners" description={t('home.hero.subhead')} lang={locale} />`
- `<SiteHeader />` — auto-detects active state from URL
- All 6 sections (hero, stats, capabilities, principles, insight, newsletter) using `t()` for copy
- `<SiteFooter />`

**Style:** Copy the scoped `<style>` block from index.astro verbatim.

**WHY hardcode locale:** pt/index.astro IS a native PT page (not a rewrite), so Astro.currentLocale would be 'pt' anyway — but hardcoding is more explicit and guarantees the i18n test ('PT home must not have EN headline') passes.

**Photo imports:** Use `../../assets/photos/` path (two levels up from src/pages/pt/).

Read src/pages/index.astro first to copy the exact body structure. The goal is a near-identical file with only the locale variable changed.
  </action>
  <verify>
    <automated>grep "useTranslations('pt')" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/pt/index.astro && grep "useTranslations('es')" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/es/index.astro && grep "hero-sp-marginal" /Users/edgardabreu/Projects/CODE/cmp-website/src/pages/pt/index.astro</automated>
  </verify>
  <done>pt/index.astro uses useTranslations('pt'); es/index.astro uses useTranslations('es'); both import hero-sp-marginal photo.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite run + build verification</name>
  <files></files>
  <action>
Run the complete verification suite:

1. `npx astro build` — must exit 0 with no errors. Check output for page count (should be 20+ pages).
2. `npx playwright test --grep @smoke` — smoke tests must all pass against dev server.
3. Review any failures and fix before proceeding to checkpoint.

If Playwright tests fail:
- PAGE-* failures: usually CSS selector mismatch — update spec to match actual rendered classes
- CONT-04 (radar index shows 2026-W20): confirm 03-01 gate flip was applied; if not, apply `published: true` to cmp-knowledge/knowledge/innovation-radar/2026-W20.md now
- I18N-05 (PT/ES headline not EN): confirm pt/index.astro and es/index.astro have correct locale

Fix all failures before marking this task done.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -5 && npx playwright test --grep @smoke 2>&1 | tail -10</automated>
  </verify>
  <done>npx astro build exits 0; npx playwright test --grep @smoke shows 0 failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — all Phase 3 pages against mock-26</name>
  <action>Human visual verification of all 7 canonical pages and collection routes. Run npm run dev and eyeball each URL listed in how-to-verify against mock-26 for pixel fidelity. No code changes required for this task — it is a human gate only.</action>
  <what-built>
    All Phase 3 pages are complete:
    - Home (/): 3-city hero rotation, stats, 4 capability cards, principles, insight, newsletter
    - Advisory (/advisory): hero, figures, 6 capability cards, engagement model, CTA bar
    - Development (/development): hero, 3 role cards, lifecycle strip
    - Platforms (/platforms): hero, 4 alternating product panels
    - Team (/team): hero, partner grid (Edgard + Gustavo), advisor bench (João Carlos, Rodrigo, Sérgio)
    - Contact (/contact): hero, 3 contact options, mailto inquiry form
    - Intelligence (/intelligence): hero, featured memo, memos archive
    - Memos (/intelligence/memos/): locale-filtered list or empty state
    - Radar (/intelligence/radar/): shows 2026-W20 entry
    - Radar detail (/intelligence/radar/2026-W20): full radar entry via RadarLayout
    - Regwatch (/intelligence/regwatch/): empty state
    - PT home (/pt/): Portuguese translations
    - ES home (/es/): Spanish translations
    - SiteHeader: active nav underline on current page
    - LocaleBanner: visible on /pt/advisory (fallback to EN with "Versão traduzida em breve")
  </what-built>
  <how-to-verify>
    Run `npm run dev` and visit each of these URLs at http://localhost:4321:

    1. http://localhost:4321/ — Home: confirm 3-city hero rotation cycles (wait 25s), 4 capability cards on cream bg, stats bar (76 / 251 / 508 / +18 GW), principles section, insight section, newsletter form
    2. http://localhost:4321/advisory — Advisory hero visible, 6 capability cards present
    3. http://localhost:4321/development — Development hero, 3 role cards
    4. http://localhost:4321/platforms — Platforms hero, 4 alternating product panels with anchor IDs
    5. http://localhost:4321/team — Team hero, 2 partners with circular photos, 3 advisors on navy section
    6. http://localhost:4321/contact — Contact hero, 3 contact option columns, form visible
    7. http://localhost:4321/intelligence — Intelligence hero, featured memo section, memos archive (empty state expected)
    8. http://localhost:4321/intelligence/radar/ — Shows "2026-W20" entry as a link
    9. http://localhost:4321/intelligence/radar/2026-W20 — Full radar entry rendered
    10. http://localhost:4321/intelligence/memos/ — Empty state "Memo library expanding"
    11. http://localhost:4321/intelligence/regwatch/ — Empty state "Regulatory watch"
    12. http://localhost:4321/pt/ — Portuguese headline (NOT "The LatAm Data Center Intelligence Firm")
    13. http://localhost:4321/es/ — Spanish headline (NOT "The LatAm Data Center Intelligence Firm")
    14. http://localhost:4321/pt/advisory — EN content visible + blue "Versão traduzida em breve" banner at top
    15. Nav links: visit /advisory and confirm "Advisory" nav link has underline/active state

    Compare each page to mock-26 at ../cmp-design-system/mock-26/ for visual fidelity.
    Note any sections that look wrong — describe specifically (e.g., "cap grid shows 1 column on desktop")
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix. Example: "approved" or "cap-grid broken: 1 column on desktop instead of 2"</resume-signal>
</task>

</tasks>

<verification>
Phase 3 complete when:
1. npx astro build exits 0
2. npx playwright test --grep @smoke: 0 failures
3. Human visual checkpoint approved
4. src/pages/image-pipeline-test.astro does not exist
5. /intelligence/radar/ shows 2026-W20 entry
6. /pt/ h1 is NOT the EN headline
</verification>

<success_criteria>
- pt/index.astro: useTranslations('pt'), same 6-section structure as EN home
- es/index.astro: useTranslations('es'), same 6-section structure as EN home
- Full Playwright smoke suite: 0 failures
- npx astro build: exits 0
- Human visual verification: all 15 URLs confirmed OK
- Phase 3 complete — ready for gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages/03-05-SUMMARY.md`
</output>
