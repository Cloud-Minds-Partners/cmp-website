---
import Base from '../layouts/Base.astro';
import SEO from '../components/SEO.astro';
---

<Base lang="en">
  <SEO
    slot="head"
    title="Cloud Minds Partners — Data Center Intelligence for Latin America"
    description="Advisory, development, and data center intelligence — applied across Brazil, Chile, and Mexico."
    image="/og/home.png"
  />

  <style slot="head" is:global>
    /* HOME-specific (literal port of mock-26/index.html) */
    .home-hero {
      position: relative;
      min-height: calc(100vh - 72px);
      display: flex; align-items: center;
      padding: 80px 0;
      isolation: isolate; overflow: hidden;
    }
    .home-hero-slides {
      position: absolute; inset: 0; z-index: -4;
      background: var(--navy-0);
    }
    .home-hero-slide {
      position: absolute; inset: 0;
      background-size: cover; background-position: center;
      opacity: 0;
      transform: scale(1.04);
      animation: slideShow 24s infinite cubic-bezier(.4,0,.2,1);
      filter: saturate(1.18) contrast(1.06) brightness(1.04);
      will-change: opacity, transform;
    }
    .home-hero-slide:nth-child(1) {
      background-image: url("/photos/hero-sp-marginal.jpg");
      animation-delay: 0s;
    }
    .home-hero-slide:nth-child(2) {
      background-image: url("/photos/hero-santiago.jpg");
      animation-delay: 8s;
    }
    .home-hero-slide:nth-child(3) {
      background-image: url("/photos/hero-cdmx.jpg");
      animation-delay: 16s;
      filter: saturate(1.18) contrast(1.06) brightness(0.95);
    }
    @keyframes slideShow {
      0%   { opacity: 0; transform: scale(1.0); }
      4%   { opacity: 1; }
      29%  { opacity: 1; }
      34%  { opacity: 0; transform: scale(1.08); }
      100% { opacity: 0; transform: scale(1.08); }
    }
    .home-hero-overlay {
      position: absolute; inset: 0; z-index: -3;
      background:
        radial-gradient(ellipse 50% 40% at 0% 70%, rgba(74, 143, 231, 0.32), transparent 60%),
        linear-gradient(180deg, rgba(5,14,29,0.05) 0%, rgba(5,14,29,0.22) 50%, rgba(5,14,29,0.78) 100%),
        linear-gradient(90deg, rgba(5,14,29,0.52) 0%, rgba(5,14,29,0.10) 55%, rgba(5,14,29,0.0) 100%);
    }
    .home-hero-grid {
      position: absolute; inset: 0; z-index: -2; pointer-events: none;
      background-image:
        linear-gradient(rgba(74,143,231,0.04) 1px, transparent 1px),
        linear-gradient(90deg, rgba(74,143,231,0.04) 1px, transparent 1px);
      background-size: 88px 88px;
      mask-image: linear-gradient(180deg, transparent, black 25%, black 70%, transparent);
    }
    .home-hero-content { position: relative; z-index: 1; max-width: 820px; margin-left: 0; }
    .home-hero .wrap {
      max-width: none; width: 100%; margin: 0;
      padding-left: clamp(32px, 5vw, 96px);
      padding-right: clamp(32px, 5vw, 96px);
    }
    .home-hero h1 {
      font-family: var(--font-display);
      font-size: clamp(36px, 4.6vw, 60px);
      line-height: 1.08;
      letter-spacing: var(--track-display);
      font-weight: 600;
      margin: 0;
      max-width: 820px;
      color: var(--cream-0);
    }
    .home-hero h1 .blue { color: var(--blue-bright); }
    .home-hero p {
      margin: 24px 0 0;
      max-width: 540px;
      font-size: 17px; line-height: 1.6;
      color: var(--cream-1);
    }
    .home-actions {
      margin-top: 36px;
      display: flex; gap: 12px; align-items: center; flex-wrap: wrap;
    }
    .home-hero-progress {
      position: absolute; bottom: 28px; right: clamp(28px, 4vw, 64px); z-index: 1;
      display: flex; gap: 4px;
    }
    .home-hero-progress-track {
      width: 56px; height: 1px; background: rgba(245, 241, 232, 0.12); position: relative; overflow: hidden;
    }
    .home-hero-progress-fill {
      position: absolute; inset: 0; background: var(--blue-bright);
      transform: scaleX(0); transform-origin: left;
      animation: progressFill 8s linear infinite;
    }
    .home-hero-progress-track:nth-child(2) .home-hero-progress-fill { animation-delay: -16s; }
    .home-hero-progress-track:nth-child(3) .home-hero-progress-fill { animation-delay: -8s; }
    @keyframes progressFill { 0% { transform: scaleX(0); } 100% { transform: scaleX(1); } }

    .stats { background: var(--black-1); border-top: 1px solid var(--line-0); border-bottom: 1px solid var(--line-0); }
    .stats::before {
      content: ""; display: block; height: 1px;
      background: linear-gradient(90deg, transparent, var(--blue-bright), transparent);
      opacity: 0.5; margin-bottom: -1px;
    }
    .stats-inner {
      display: grid; grid-template-columns: repeat(4, 1fr);
      max-width: var(--max-w); margin: 0 auto; padding: 0 var(--gutter);
    }
    .stat { padding: 60px 32px; border-right: 1px solid var(--line-0); text-align: center; }
    .stat:last-child { border-right: 0; }
    .stat-num {
      font-family: var(--font-display);
      font-size: clamp(40px, 4.6vw, 64px);
      line-height: 0.95; font-weight: 600;
      letter-spacing: var(--track-tight);
      color: var(--cream-0);
      font-feature-settings: "tnum", "lnum";
      display: flex; align-items: baseline; gap: 6px; justify-content: center;
    }
    .stat-num .unit { font-family: var(--font-body); font-size: 14px; color: var(--cream-2); font-weight: 500; letter-spacing: 0; margin-left: 6px; }
    .stat-label { margin: 14px auto 0; font-size: 13.5px; color: var(--cream-1); line-height: 1.5; max-width: 240px; }
    .stats-attrib { max-width: var(--max-w); margin: 0 auto; padding: 16px var(--gutter) 32px; font-size: 12px; color: var(--cream-3); text-align: center; letter-spacing: 0.02em; }

    .capabilities { padding: clamp(96px, 14vh, 160px) 0; background: var(--cream-bg); color: var(--navy-0); }
    .capabilities .cap-grid {
      margin-top: 56px;
      grid-template-columns: repeat(3, 1fr);
    }
    .cap-see-all {
      display: inline-block;
      padding: 14px 28px;
      border: 1px solid var(--navy-0);
      border-radius: 999px;
      font-size: 14px; font-weight: 600;
      color: var(--navy-0);
      transition: background var(--t-fast), color var(--t-fast);
    }
    .cap-see-all:hover { background: var(--navy-0); color: var(--cream-bg); }

    .insight {
      background: var(--cream-bg);
      color: var(--navy-0);
      padding: clamp(96px, 14vh, 160px) 0;
      border-top: 1px solid rgba(5,14,29,0.06);
    }
    .insight-inner {
      display: grid; grid-template-columns: 1.05fr 1fr; gap: clamp(48px, 6vw, 96px);
      max-width: var(--max-w); margin: 0 auto; padding: 0 var(--gutter);
      align-items: center;
    }
    .insight-img {
      aspect-ratio: 1 / 1;
      position: relative; overflow: hidden;
      border: 1px solid rgba(5,14,29,0.08);
      border-radius: 12px;
      background: #0a1d3a;
      box-shadow: 0 24px 48px rgba(5,14,29,0.12);
    }
    .insight-img-bg {
      position: absolute; inset: 0;
      background: url("/photos/pulse-06-cover.png") center / cover no-repeat #0a1d3a;
    }
    .insight-tag {
      position: absolute; bottom: 18px; right: 18px; z-index: 2;
      font-family: var(--font-body); font-size: 12px; font-weight: 700;
      letter-spacing: 0.06em; text-transform: uppercase;
      color: #ffffff;
      background: #0d9488;
      padding: 8px 14px; border-radius: 999px;
      box-shadow: 0 6px 18px rgba(13, 148, 136, 0.45);
    }
    .insight-tag::before {
      content: ""; display: inline-block; width: 6px; height: 6px;
      border-radius: 50%; background: #ffffff; margin-right: 8px;
      vertical-align: middle;
      animation: pulse 2.2s ease-out infinite;
    }
    .insight .section-eyebrow { color: var(--blue-deep); }
    .insight .section-eyebrow::before { background: var(--blue-deep); }
    .insight h3 {
      font-family: var(--font-display);
      font-size: clamp(26px, 3.2vw, 40px); line-height: 1.1;
      letter-spacing: var(--track-display); font-weight: 600;
      margin: 0; color: var(--navy-0);
      max-width: 580px;
    }
    .insight h3 .blue { color: var(--blue); }
    .insight-inner > div > p { margin: 24px 0 0; font-size: 16px; line-height: 1.65; color: #4D5F7A; max-width: 500px; }
    .insight-meta {
      margin-top: 36px; display: flex; gap: 32px;
      padding-top: 24px; border-top: 1px solid rgba(5,14,29,0.10);
    }
    .insight-meta span { display: flex; flex-direction: column; gap: 6px; font-family: var(--font-body); font-size: 12.5px; letter-spacing: 0; text-transform: none; color: #6B7B92; }
    .insight-meta strong { color: var(--navy-0); font-weight: 600; letter-spacing: 0; text-transform: none; font-family: var(--font-body); font-size: 14.5px; }
    .insight-cta {
      margin-top: 32px;
      display: inline-flex; align-items: center; gap: 12px;
      font-size: 14px; color: var(--blue); font-weight: 600;
      padding-bottom: 4px; border-bottom: 1px solid var(--blue);
      transition: gap var(--t-fast), color var(--t-fast);
    }
    .insight-cta:hover { gap: 18px; color: var(--navy-0); }

    @media (max-width: 1100px) {
      .stats-inner { grid-template-columns: repeat(2, 1fr); }
      .stat { padding: 36px 24px 36px 0; }
      .stat:nth-child(2) { padding-right: 0; border-right: 0; }
      .stat:nth-child(3) { border-top: 1px solid var(--line-0); }
      .stat:nth-child(4) { border-top: 1px solid var(--line-0); border-right: 0; padding-right: 0; }
    }
    @media (max-width: 880px) {
      .stats-inner, .cap-grid, .insight-inner { grid-template-columns: 1fr; }
      .insight-img { aspect-ratio: 16 / 10; }
    }
  </style>

  <nav class="nav">
    <div class="nav-inner">
      <a class="brand" href="/" aria-label="Cloud Minds Partners — home">
        <img class="brand-wordmark" src="/logo/cmp-wordmark.png" alt="Cloud Minds Partners" />
        <span class="brand-tag">The LatAm DC intelligence firm.</span>
      </a>
      <div class="nav-links">
        <a href="/advisory">Advisory</a>
        <a href="/development">Development</a>
        <a href="/intelligence">Intelligence</a>
        <a href="/platforms">Platforms</a>
        <a href="/team">Team</a><a href="/contact">Contact</a>
      </div>
      <div class="nav-right">
        <div class="nav-lang">
          <a href="/" class="active">EN</a><span class="sep">·</span><a href="/pt/">PT</a><span class="sep">·</span><a href="/es/">ES</a>
        </div>
        <a href="mailto:info@cloudmindspartners.com" class="nav-cta">
          Talk to us
          <svg width="12" height="12" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M1 7h12M7 1l6 6-6 6"/></svg>
        </a>
      </div>
    </div>
  </nav>

  <header class="home-hero">
    <div class="home-hero-slides" aria-hidden="true">
      <div class="home-hero-slide"></div>
      <div class="home-hero-slide"></div>
      <div class="home-hero-slide"></div>
    </div>
    <div class="home-hero-overlay" aria-hidden="true"></div>
    <div class="home-hero-grid" aria-hidden="true"></div>

    <div class="wrap">
      <div class="home-hero-content">
        <h1>Data center intelligence for <span class="blue">Latin&nbsp;America.</span></h1>
        <p>Advisory, development, and data center intelligence — based in Brazil, Chile, and Mexico, applied across all of LatAm.</p>
        <div class="home-actions">
          <a class="btn-primary" href="mailto:info@cloudmindspartners.com">
            Engage advisory
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M1 7h12M7 1l6 6-6 6"/></svg>
          </a>
          <a class="btn-outline" href="/platforms">Open the platforms →</a>
        </div>
      </div>
    </div>

    <div class="home-hero-progress" aria-hidden="true">
      <div class="home-hero-progress-track"><div class="home-hero-progress-fill"></div></div>
      <div class="home-hero-progress-track"><div class="home-hero-progress-fill"></div></div>
      <div class="home-hero-progress-track"><div class="home-hero-progress-fill"></div></div>
    </div>
  </header>

  <section class="stats" aria-label="Coverage statistics">
    <div class="stats-inner">
      <div class="stat reveal">
        <div class="stat-num"><span class="stat-value">76</span><span class="unit">cities</span></div>
        <div class="stat-label">Working portfolio</div>
      </div>
      <div class="stat reveal reveal-delay-1">
        <div class="stat-num"><span class="stat-value">251</span><span class="unit">sites</span></div>
        <div class="stat-label">Greenfield pipeline</div>
      </div>
      <div class="stat reveal reveal-delay-2">
        <div class="stat-num"><span class="stat-value">508</span><span class="unit">studies</span></div>
        <div class="stat-label">Grid intelligence</div>
      </div>
      <div class="stat reveal reveal-delay-3">
        <div class="stat-num"><span class="stat-value">+18</span><span class="unit">GW</span></div>
        <div class="stat-label">Headroom 2028</div>
      </div>
    </div>
    <p class="stats-attrib">CMP canonical datasets — ONS, EPE, Land Intel, SST · updated continuously</p>
  </section>

  <section class="capabilities on-cream" id="platforms">
    <div class="wrap">
      <div class="reveal" style="text-align:center; margin-bottom: 24px;">
        <div class="section-eyebrow" style="justify-content:center;">Platforms</div>
        <h2 class="section-heading" style="margin: 0 auto; max-width: 720px;">Three tools. <span class="blue">In production.</span></h2>
        <p class="section-sub" style="margin: 24px auto 0; max-width: 560px;">Built for our advisory. Available to clients. Live data — not a slide deck.</p>
      </div>

      <div class="cap-grid">
        <a class="cap-card reveal reveal-delay-1" href="/platforms#site-screening-tool">
          <div class="cap-card-photo" style="background-image:url('/photos/card-home-site-selection.jpg');"></div>
          <div class="cap-card-body">
            <div class="cap-num">01 — Site</div>
            <h3 class="cap-title">Site Screening Tool</h3>
            <p class="cap-desc">Score 76+ LatAm cities across 8 criteria — land, grid, water, fiber, climate, fiscal, regulatory, environmental.</p>
          </div>
        </a>

        <a class="cap-card reveal reveal-delay-2" href="/platforms#grid-intelligence">
          <div class="cap-card-photo" style="background-image:url('/photos/card-home-grid-intel.jpg');"></div>
          <div class="cap-card-body">
            <div class="cap-num">02 — Grid</div>
            <h3 class="cap-title">Grid Intelligence</h3>
            <p class="cap-desc">Brazil live. Chile and Mexico underway. Grid analysis, N-1 headroom, and strategic power overview across LatAm.</p>
          </div>
        </a>

        <a class="cap-card reveal reveal-delay-1" href="https://dcinsights.web.app">
          <div class="cap-card-photo" style="background-image:url('/photos/card-home-dc-insights.jpg');"></div>
          <div class="cap-card-body">
            <div class="cap-num">03 — Editorial</div>
            <h3 class="cap-title">DC Insights</h3>
            <p class="cap-desc">Daily LatAm DC newsletter. Primary-source, audited, free.</p>
          </div>
        </a>
      </div>

      <div class="reveal reveal-delay-2" style="margin-top: 48px; text-align: center;">
        <a href="/platforms" class="cap-see-all">See 2 more in development →</a>
      </div>
    </div>
  </section>

  <section class="principles on-cream">
    <div class="wrap">
      <div class="reveal" style="text-align:center; margin-bottom: 24px;">
        <div class="section-eyebrow" style="justify-content:center;">How we work</div>
        <h2 class="section-heading" style="margin: 0 auto; max-width: 680px;">Three principles. <span class="blue">Non-negotiable.</span></h2>
      </div>
      <div class="principles-grid reveal reveal-delay-1" style="margin-top: 64px;">
        <div class="principle">
          <div class="principle-num">01</div>
          <h4>Primary sources only</h4>
          <p>Every claim links to a document we can hand a regulator.</p>
        </div>
        <div class="principle">
          <div class="principle-num">02</div>
          <h4>LatAm on the ground.</h4>
          <p>Engagements run from Brazil, Chile, and Mexico — covering all of LatAm.</p>
        </div>
        <div class="principle">
          <div class="principle-num">03</div>
          <h4>Audited before delivery</h4>
          <p>Chain of Truth review. If we can't verify, we drop it.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="insight" id="intelligence">
    <div class="insight-inner">
      <div class="insight-img reveal">
        <div class="insight-img-bg" aria-hidden="true"></div>
        <span class="insight-tag">Pulse 06 · This week</span>
      </div>
      <div class="reveal reveal-delay-1">
        <div class="section-eyebrow">Published weekly</div>
        <h3>DC Weekly Pulse — <span class="blue">every Tuesday</span>.</h3>
        <p>A weekly video brief on the signals moving the LatAm data center market. Primary sources only. Audited claim-by-claim. Free to watch.</p>
        <div class="insight-meta">
          <span><strong>Pulse</strong>Weekly · LatAm</span>
          <span><strong>6 editions</strong>Since Apr 2026</span>
          <span><strong>~1 min</strong>Each</span>
        </div>
        <a href="/intelligence" class="insight-cta" aria-label="Watch the latest DC Weekly Pulse">
          Watch latest
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M1 7h12M7 1l6 6-6 6"/></svg>
        </a>
      </div>
    </div>
  </section>

  <footer>
    <div class="foot-links">
      <div class="foot-brand-cell">
        <a class="brand" href="/"><img class="brand-wordmark" src="/logo/cmp-wordmark.png" alt="Cloud Minds Partners" /></a>
        <p>Data center intelligence for Latin America.</p>
        <div class="foot-social" aria-label="Follow us">
          <a href="https://www.linkedin.com/company/cloud-minds-partners" aria-label="LinkedIn" target="_blank" rel="noopener"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M20.45 20.45h-3.55v-5.57c0-1.33-.03-3.04-1.85-3.04-1.86 0-2.14 1.45-2.14 2.95v5.66H9.36V9h3.41v1.56h.05c.48-.9 1.64-1.85 3.37-1.85 3.6 0 4.26 2.37 4.26 5.45v6.29zM5.34 7.43a2.06 2.06 0 1 1 0-4.12 2.06 2.06 0 0 1 0 4.12zM7.12 20.45H3.56V9h3.56v11.45zM22.23 0H1.77C.79 0 0 .77 0 1.72v20.56C0 23.23.79 24 1.77 24h20.46c.97 0 1.77-.77 1.77-1.72V1.72C24 .77 23.2 0 22.23 0z"/></svg></a>
          <a href="https://wa.me/5511915788796" aria-label="WhatsApp" target="_blank" rel="noopener"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M17.5 14.4c-.3-.1-1.8-.9-2-1-.3-.1-.5-.2-.7.2-.2.3-.8 1-.9 1.2-.2.2-.3.2-.6.1-.3-.1-1.3-.5-2.4-1.5-.9-.8-1.5-1.8-1.7-2.1-.2-.3 0-.5.1-.6.1-.1.3-.4.5-.6.1-.2.2-.3.3-.5.1-.2 0-.4 0-.5 0-.1-.7-1.6-.9-2.2-.2-.6-.5-.5-.7-.5h-.6c-.2 0-.5.1-.7.4-.3.3-1 1-1 2.4 0 1.4 1 2.8 1.2 3 .1.2 2 3 4.8 4.2.7.3 1.2.5 1.6.6.7.2 1.3.2 1.8.1.5-.1 1.7-.7 2-1.4.2-.7.2-1.3.2-1.4 0-.1-.3-.2-.5-.3zM12 22a10 10 0 0 1-5-1.3L2 22l1.3-5A10 10 0 1 1 12 22zm0-18.2A8.2 8.2 0 0 0 4.6 16l-.2.5-.8 3 3-.8.5.2c1.2.7 2.6 1.1 4 1.1 4.5 0 8.2-3.7 8.2-8.2S16.5 3.8 12 3.8z"/></svg></a>
        </div>
      </div>
      <div><h4>Platforms</h4><ul><li><a href="/platforms">Site Screening Tool</a></li><li><a href="/platforms">Grid Intelligence</a></li><li><a href="/platforms">DC Insights</a></li><li><a href="/platforms">DC Test Fit Pro</a></li><li><a href="/platforms">DC Financial Model</a></li></ul></div>
      <div><h4>Intelligence</h4><ul><li><a href="/intelligence">Technical Memos</a></li><li><a href="/intelligence">Innovation Radar</a></li><li><a href="/intelligence">Regulatory Watch</a></li><li><a href="/intelligence">Weekly Pulse</a></li></ul></div>
      <div><h4>Company</h4><ul><li><a href="/advisory">Advisory</a></li><li><a href="/development">Development</a></li><li><a href="/team">Team</a><a href="/contact">Contact</a></li></ul></div>
      <div><h4>Contact</h4><ul><li><a href="mailto:info@cloudmindspartners.com">info@cloudmindspartners.com</a></li><li>São Paulo · BR</li><li>Santiago · CL</li><li>Mexico City · MX</li></ul></div>
    </div>
    <div class="foot-bottom"><span>© 2026 Cloud Minds Partners</span><span>All rights reserved.</span></div>
  </footer>

  <script is:inline>
    const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });
    document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

    if (!reducedMotion) {
      const animateNum = (el, target, duration = 1400) => {
        const isPlus = String(el.textContent).trim().startsWith('+');
        const abs = Math.abs(target);
        const start = performance.now();
        const step = (now) => {
          const t = Math.min((now - start) / duration, 1);
          const eased = 1 - Math.pow(1 - t, 3);
          const val = Math.floor(abs * eased);
          el.textContent = (isPlus ? '+' : '') + val;
          if (t < 1) requestAnimationFrame(step);
          else el.textContent = (isPlus ? '+' : '') + abs;
        };
        requestAnimationFrame(step);
      };
      const statObs = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const values = entry.target.querySelectorAll('.stat-value');
            const targets = [76, 251, 508, 18];
            values.forEach((n, i) => animateNum(n, targets[i]));
            statObs.disconnect();
          }
        });
      }, { threshold: 0.35 });
      const statsEl = document.querySelector('.stats');
      if (statsEl) statObs.observe(statsEl);
    }
  </script>
</Base>
